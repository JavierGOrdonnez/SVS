# SPEC-migration.md — migration module

Part of the SVS spec. Top-level goal/shared constraints/invariants/roadmap/bug
log live in [`SPEC.md`](../../SPEC.md); this file owns §T tasks T11,
T-mig-tab, T43, T44 and this module's domain-specific §V/§I content only.

---

## §I — Interfaces (migration-specific)

```
file: data/raw/migration_spain.csv                     → migration flows & stock 2000-2025: by sex, age, origin nationality, foreign/Spanish split (T11, 2337 rows; merged from claude/spain-migration-data-PsIYr 2026-06-07); joint age(5yr)×sex×citizenship cross for top-50 nationalities (flow 1998-2024, stock 2002-2025) added via `src/migration/parse_eurostat_migration_cohort.py` from Eurostat SDMX-JSON API `migr_imm1ctz`/`migr_pop1ctz` — series `stock_nationality` (renamed from `stock_foreign_nationality`, per-country, includes ES for feminicide denominator); `flow_immigration_from_abroad` (per-country); ~86% coverage of INE ECP total foreign stock (V42, B34); also carries `Y_LT15`/`Y_LT5`/`Y_GE65`/`Y_GE85` aggregate age bands (age_group `0-14`/`0-4`/`65+`/`85+`) plus fine 5yr bands `5-9` through `80-84` for the T69/T70 age pyramids — the `5-9`, `60-64`...`80-84`/`Y_GE85`, and `Y_LT5` bands all required live Eurostat API pulls (`data/raw/eurostat_migr_pop1ctz_es.jsonl` originally only carried bands from 10-14 through 55-59)
file: data/sources/migracion_espana.md                 → migration source doc — INE EMCR/EVR/Padrón/ECP, Eurostat, MISSM table refs + caveats (2008, 2021 method breaks)
cmd:  `uv run python src/migration/parse_eurostat_migration_cohort.py <flow.jsonl> <stock.jsonl>` → Eurostat SDMX-JSON API `migr_imm1ctz`/`migr_pop1ctz` (pre-downloaded JSONL, not bulk TSV) → appends to `migration_spain.csv` (T11,T43,T44,T66)
file: `src/migration/build_dashboard_data.py` (formerly `build_migration_dashboard_data.py`) → `build()` reads `migration_spain.csv`, consumed by `src/analysis/build_dashboard.py` → `docs/data/migration.json` (T-mig-tab)
```

## §V — Invariants (migration-specific)

V44: any dashboard drill-down series (a finer breakdown of an already-displayed aggregate, e.g. T72's per-country lines under `mi-stock-region`'s per-region lines) ! be derived by summing the same underlying rows the aggregate itself sums (`stock_nationality` in T68/T72's case), not computed as a separately-maintained series — guarantees the drill-down's parts always sum exactly to the aggregate line, with no double-source-of-truth drift possible between the two views.

V25: recent-arrival-cohort population (no direct tenure-stratified table exists) ! approximated as cumulative net migration inflow over the trailing ~3yr window, joined against `migration_spain.csv` (T11) age-band × sex × origin-nationality data (closest available band to 15-59 male); settled_pop = stock − cohort_pop; ⊥ presented without documenting the approximation + its caveats (naturalization, re-emigration, irregular-migration undercount all bias in either direction). UPDATE 2026-07-01: for Morocco (MA) and Algeria (DZ) specifically, `migration_spain.csv` now carries the *real* joint 5yr-age-band × sex × citizenship cross (flow 1998-2024, stock 2002-2025, via Eurostat `migr_imm1ctz`/`migr_pop1ctz` — INE's own tables 24290/36825 only reach 2021/2022) — no extrapolation needed for T41's cohort_pop/settled_pop split for these two countries; the cumulative-inflow approximation above still applies wherever this direct cross is absent (other nationalities in T42's broader correlation scope). UPDATE 2026-07-20: expanded to top-49 nationalities (T43,T44,T66); `stock_nationality` series now carries per-year per-nationality sex-specific stock for ~86% of INE ECP foreign stock (V42). UPDATE 2026-07-22: re-running `parse_eurostat_migration_cohort.py` (T69) against the already-downloaded JSONL picked up Romania (RO), which had full Eurostat coverage but had been missing from `migration_spain.csv` since a stale regeneration — count is now 50 nationalities, not 49; AT remains absent (never downloaded). UPDATE 2026-07-23: T69/T70 pyramids extended to a 17-band scale (0-4, 5-9, 10-14, ..., 75-79, 80+) — the `5-9`, `60-64`/`65-69`/`70-74`/`75-79`/`80-84`/`Y_GE85`, and `Y_LT5` (0-4) Eurostat age bands were not in the originally pre-downloaded JSONL (only `10-14` through `55-59` + the `Y_GE65` aggregate) despite being available live from `migr_pop1ctz`; live-fetched for all 50 nationalities and appended to `data/raw/eurostat_migr_pop1ctz_es.jsonl`. The old `0-9` band (derived as `0-14 minus 10-14`) is gone — `0-4`/`5-9` are now both real Eurostat/population bins, no derivation needed.

## §T — Tasks (migration)

| id | status | track | task | cites |
|---|---|---|---|---|
| T11 | ~ | C,infra | Populate `data/raw/migration_spain.csv` (2337 rows) — flows 2000-2024, foreign-nationality stock 2000-2025, top-10 origin nationalities for 2008-2024 intake & 2025 stock (sex-split sums match published totals exactly 2008-2024), broad age bands 2008-2024 + granular 5-yr bands 2024, SS-affiliation snapshots; EXTENDED 2026-07-01: joint age(5yr)×sex×citizenship cross for Morocco/Algeria (flow 1998-2024, stock 2002-2025) via `src/migration/parse_eurostat_migration_cohort.py` (Eurostat `migr_imm1ctz`/`migr_pop1ctz` bulk, cross-validated exact match against pre-existing MA totals 2022-2024). EXTENDED 2026-07-20: expanded to top-49 nationalities (T43,T44,T66); series renamed `stock_foreign_nationality` → `stock_nationality` (includes ES for feminicide denominator). EXTENDED 2026-07-22: regeneration for T69 restored Romania (RO), for 50 nationalities total; added `Y_LT15`/`Y_GE65` aggregate age bands. See `data/sources/migracion_espana.md` | V10,C8,V25,V42 |
| T-mig-tab | x | infra | Rebuild migration dashboard tab on current `docs/index.html` architecture (T17/T23 tabbed architecture) — charts G1 (inflow + 2008 break), G2 (origin composition), G3 (sex split), G4/G5 (age composition/profile), G6 (stock); delivered via `src/migration/build_dashboard_data.py` (commit `55cfa64`). | I.*,V1,V6 |
| T43 | x | D | Obtain real age×sex×citizenship joint-cross migration/stock data — South America. Extended `src/migration/parse_eurostat_migration_cohort.py`'s `CITIZENS` list to add CO, VE, PE, HN, AR, EC, PY, BR, BO, CU, NI, DO (T41 South America group; MIR name-instability caveats from T42 apply when mapping names→codes); re-run against Eurostat SDMX-JSON API migr_imm1ctz/migr_pop1ctz to get the real 5yr-age-band × sex × citizenship joint cross per country; appended to `migration_spain.csv` (stock_nationality series). | V6,V25,C10 |
| T44 | x | D | Obtain real age×sex×citizenship joint-cross migration/stock data — EU-Europe excl. Spain. Extended `src/migration/parse_eurostat_migration_cohort.py`'s `CITIZENS` list to add RO, IT, DE, FR, PT, BG, NL, PL, SE, IE (T41 EU-Europe group); same Eurostat SDMX-JSON API source/process as T43 (run together in one script invocation); appended to `migration_spain.csv` (stock_nationality series). | V6,V25,C10 |
| T66 | x | A | Expand Eurostat extraction to top-35 nationalities + ES (94.4% of foreign stock by2025 total) for feminicide rate denominators. Renamed series `stock_foreign_nationality` → `stock_nationality` (includes ES); `compute_feminicide_rates.py` now sums per-year per-nationality sex-specific stock_nationality rows for foreign-population denominators (B34), replacing the frozen 2025 female-share ratio. CI removed from rates (B35). | V42,V43,B34,B35 |
| T67 | . | D,opt | Rewrite `parse_eurostat_migration_cohort.py` using Pandas — replace manual JSONL loading + per-row dict filtering with `pd.read_json()` / DataFrame operations; drop 18 countries from CITIZENS list that have no stock data in Eurostat migr_pop1ctz (EE, EG, EL, ER, HR, HU, IR, JP, KR, LU, LV, MT, SI, SK, SY, TH, TN, TR); add batch-completeness validation; clean up idempotency guard to preserve INE-sourced rows precisely. Optional quality improvement, not blocking. | V42 |
| T68 | x | D | Stock by region over time — added `REGION_MAP` (nationality→region name, 50 codes) + `_stock_by_region()` to `build_dashboard_data.py`; filters `stock_nationality` to `age_group=all, sex=all`; sums value per region × year; outputs `stock_by_region` (`{ years, regions, series: { "Africa": [...], ... } }`). New panel `mi-stock-region` (multi-line chart, one line per region, years on x-axis). Regions: Africa (MA DZ SN NG ML GQ GN GH GM), Latin America (CO VE PE HN AR EC PY BR BO CU NI DO CL MX UY), Anglo (UK US), EU (RO IT DE FR PT BG NL PL SE IE BE DK FI LT AT), Non-EU Europe (UA RU CH NO MD), Asia (CN PK IN BD PH); AT contributes 0 (no Eurostat rows downloaded). | V42 |
| T69 | x | D | Foreign age pyramid — extended `parse_eurostat_migration_cohort.py`'s `_age_to_group()` to map `Y_LT15`→`0-14`, `Y_LT5`→`0-4`, `Y_GE65`→`65+`, `Y_GE85`→`85+`, plus fine 5yr bands `Y5-9`...`Y80-84` (all live-downloaded from Eurostat migr_pop1ctz and appended to `data/raw/eurostat_migr_pop1ctz_es.jsonl`, since the pre-downloaded file only carried bands from 10-14 through 55-59); re-ran the parser (also picked up Romania, absent from the prior CSV — see T11/V25 changelog, B36); `build_dashboard_data.py._stock_age_pyramid()` derives 80+ as `80-84 plus 85+` (0-4/5-9 are real bands now, no derivation); outputs `stock_age_pyramid` (`{ year, ages, male, female, regions: { "Africa": { male: [...], female: [...] }, ... } }`) for `year`=2024 (capped by T70's population source). New panel `mi-age-pyramid`: horizontal stacked bars by region, males left (negative), females right, age bands on y-axis, fine 5yr bands 0-4 through 75-79 then 80+ aggregate; `grouped: false` on every dataset so male/female bars share the same row instead of splitting it top/bottom. | V42 |
| T70 | x | D | Spanish age pyramid — `_spanish_age_pyramid()` derives Spanish-national population by age × sex from `population_spain_midyear_5yr.csv` (year 2024, the latest available) minus `stock_nationality` (foreign, summed across all 50 nationalities) for matching age bands; population's native `<1`/`1-4` bins roll up into `0-4`, `5-9` maps 1:1, `80-84`/`85-89`/`90-94`/`95+` roll up into `80+`. Both pyramids share the same 17-band scale (0-4, 5-9, 10-14, ..., 75-79, 80+) and year (2024). New panel `mi-age-pyramid-es`: same layout as T69 but for Spanish nationals only (single color, not stacked by region; same `grouped: false` fix). | V42 |
| T71 | x | D | Updated `cav-mig` age-stratification caveat: "exists only for Morocco and Algeria" → "now cover 50 nationalities (T43/T44/T66), ~94% of foreign stock", plus a note on the T70 pyramid's subtraction approximation and Jan-1/Jul-1 reference-date mismatch. | |
| T72 | . | D | Per-country drill-down on `mi-stock-region` (T68) — extend `_stock_by_region()` to also emit a per-nationality breakdown, summed from the same `stock_nationality` rows already rolled up into the region totals (guarantees per-country lines sum exactly to their region's aggregate, per V44); extend `COUNTRY_NAMES` (currently 9 entries, for `TOP_ORIGINS` only) to cover all 50 `REGION_MAP` codes, reconciling the existing `UK`/`GB` code mismatch between the two dicts. Chart.js 4.4.4 `legend.onClick` (click a region's legend entry) swaps that region's line for its constituent countries' lines, cycling `PALETTE` (8 colors vs. up to 15 countries in Latin America/EU — accepted color-repeat tradeoff, not blocking); a "‹ back to regions" control rendered into the currently-unused `.cp-legend` slot (`chart-panel.js`) restores the 6-region view. First canvas-click/drill-down interaction in `docs/app.js` — every existing interactive control (`tab-bar.js`, `confidence-filter.js`) is a plain DOM button dispatching a `CustomEvent`, not a Chart.js click handler, so this is new plumbing, not a reuse. `AT` (zero Eurostat rows, per T68) rendered dimmed/flat-0 like other data-gap series, not hidden. | V42,V44,T68 |

## Related bugs

B34, B35 — see `SPEC.md` §B for the full centralized log.
