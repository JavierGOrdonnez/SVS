# SPEC.md — SVS (Spain Violence Statistics)

---

## §G — Goal

Compute annual, 5-year & lifetime cumulative probability of rape/sexual-assault/femicide/homicide/non-sexual-violence for woman born 2000 ∈ Spain, holding 2025 conditions fixed. Extend: estimate partial effects of covariates (far-right vote share, immigration volume & composition) using 2000–2025 historical series.

## Key Quantities & Insights
- **A. Victim risk profile** — for rape / sexual-assault / femicide / other-homicide / non-sexual-violence + combined "any violence": incidence/100k (2025) → 1yr/5yr/lifetime cumulative P, both **reported** and **dark-figure-corrected** (T8, T9).
- **B. Historical trends** 2000–2025 with definition-break annotations (largely in the dashboard already).
- **C. Covariate effects** (associative) — violence-rate ~ far-right vote-share + immigration volume/composition; ±10/20% scenario table (T10, T12, T13).
- **D. Peligrosity (umbrella)** — per-capita perpetrator rate, **bracket [convicted ↔ identified] per 100k men**, by age × nationality × origin (later socioeconomic). Always paired with absolute framing per V14. The migrant/nationality slice = T26–T29; new general engine = T30/T31; tenure/cohort-specific test = T41; evolution+correlation charts = T42.
- **E. Victim–aggressor relationship structure** — relationship distribution + victim↔aggressor matrix (sexual assault, ±penetration); produces adjustment factors that feed back into A and D (T32, T33).
- **F. Hate-speech & Migration correlation** — analyze hate-speech trends in Spain + correlates (media presence, polling/worry, xenophobic political party voting, migration-associated crime data) via watchdog reports & primary sources (T37–T40).
16: - **G. GBV non-sexual justice funnel** (*later expansion*) — denuncias → diligencias → condenas + reporting rate, for physical/psychological GBV (T34).

---

## §C — Constraints

C1: All claimed statistics ! cite exact source + table/figure + year of publication.  
C2: Reported-crime data ≠ actual incidence; dark-figure correction ! attempted via survey cross-validation.  
C3: Definition of each offence ! documented per year — Spanish penal code changed materially in 2022 (LO 10/2022 "Solo sí es sí") & 2023 reform; pre/post comparability ⊥ assumed without explicit bridge.  
C4: "Femicide" in official data = intimate-partner/ex-partner homicide only (Delegación del Gobierno registry) ≠ all female homicides ≠ academic broad definition.  
C5: Macroencuesta (victimisation survey) ! used to bound dark figure; conducted irregularly (2011, 2015, 2019); interpolation between waves ? unreliable.  
C6: Age-specific rates ! used wherever available; aggregate rates ∈ fallback only.  
C7: ∀ probability estimates → competing-risks life-table, not naive annual-rate multiplication (events not independent across years).  
C8: Covariate regression = associative, ⊥ causal.  
C9: Prior AI model (Haiku) likely hallucinated point estimates without source verification → ∀ numbers from that conversation ! re-verified against primary sources before use.  
C10: Population denominator ! female population of Spain from INE Padrón; age-stratified where possible.  
C11: Ethnicity/nationality breakdowns sparse in official data; include when available, flag absence.  
C12: ∀ CSV rows ! have `confidence` ∈ {high, medium, low, unverified} — unverified rows ⊥ used in final estimates until confirmed.  
C13: PDF extraction parsers ! assume stable table formats: Delegación del Gobierno 1-page format (2003–2026), MIR Informe standard tables (2019–2024), MIR Anuario TABLA 3-1-5/3-1-6 dense whitespace-aligned text layout — no dedicated per-category chapter, no ruling lines (2021–2023 PDFs sourced so far; 2000–2020 gap, no PDFs obtained yet); format deviations ⊥ silently skipped; parser ! halt and report inconsistency. Source PDFs for all MIR series (Anuario, Informe delitos libertad sexual, Balance de Criminalidad trimestral, Informes de Delitos de Odio) ! sourced from `https://estadisticasdecriminalidad.ses.mir.es/publico/portalestadistico/publicaciones.html` — the SES portal is the canonical hub for these; per-year direct URLs go in the relevant `data/sources/*.md` doc, not here.  
C14: Python parser stack requires pdfplumber (table extraction), pandas (aggregation + validation), pydantic (schema validation for nested JSON exports), openpyxl (fallback for Excel tables); validation failures ! block output (no partial extracts).  
C15: Literature-first: published studies/syntheses ! preferred where they exist, but ∀ secondary figure → primary source ! traced & documented; secondary value ⊥ cited as if primary. (extends C1; the 29-source catalogue in `fuentes_secundarias_analisis_espana.md` is the working set.)

---

## §R — Research

R1: Eurostat vs INE migration/population parsers — investigated whether
`parse_eurostat_migration_cohort.py`, `parse_ine_population.py`,
`parse_ine_mortality.py`, `parse_ine_tabla28716.py` are near-duplicate scripts
that should be merged. Verdict: **not duplicative** — each targets a distinct
source format + delivery mechanism, and only one (`parse_eurostat_migration_cohort.py`)
touches migration at all; there is no separate INE-migration parser script in
`src/` to compare it against (`migration_spain.csv`'s base T11 data, 2337 rows,
was built in a merged branch, not a script that lives here today).

| | `parse_eurostat_migration_cohort.py` | `parse_ine_population.py` | `parse_ine_mortality.py` | `parse_ine_tabla28716.py` |
|---|---|---|---|---|
| source | Eurostat bulk SDMX TSV (`migr_imm1ctz`/`migr_pop1ctz`), manual gunzip download, not committed | INE table 56934 CSV, semicolon-delimited, `;` sep | INE table 7947, JSON API dump (`wstempus`) | INE table 28716 CSV, semicolon-delimited |
| fetch | manual (large bulk file, user downloads + gunzips) | manual CSV (could be scripted, isn't) | manual JSON dump | **live HTTP fetch** via `requests` inside the script |
| number format | plain int + trailing flag chars (`b`,`e`,`p`,`@N`) stripped by regex | Spanish thousands-dot, some post-2021 Censo rows plain int — both handled inline | plain int (JSON) | Spanish thousands-dot + decimal-comma, `pandas`-based |
| dimension shape | wide TSV, one column per year, dims packed into first column comma-joined | long CSV, one row per (age, sex, period) | nested JSON, `nombre` field encodes sex+age+cause in two inconsistent string formats (regex-split) | long CSV, one row per (crime_type, nationality, year) |
| output | appends to shared `migration_spain.csv` (idempotent: drops+regenerates its own joint-cross rows on rerun) | writes 2 new CSVs (`population_spain_estimates.csv` full long form, `population_spain_midyear_5yr.csv` binned to match mortality table's age bins) | writes 1 new CSV, columns include a `series_cod` for traceability | writes 2 new CSVs (raw counts + derived nationality %) |
| why separate | different bulk-download source entirely (Eurostat, not INE); different join granularity (age×sex×citizenship cross INE itself doesn't publish past 2021/2022) | population is a distinct target quantity (denominator) from migration flow/stock | mortality is a distinct target quantity again, and INE's JSON API differs structurally from its CSV exports | crime convictions by nationality is a distinct target quantity; also the only one of the four with a live network fetch built in |

Conclusion: the shared pattern across all four is "some INE/Eurostat tabular
dump → tidy CSV", but the actual parsing logic (delimiter, number format,
dimension encoding, fetch mechanism) differs enough per source that a merged
parser would need a source-specific branch for every step anyway — same
non-duplication verdict as T53 reached for `src/parsers/`'s shared `utils.py`
extraction (that consolidation targeted truly identical logic — pdftotext
subprocess calls, CSV header guards — not source-specific parsing).

---

## §I — Interfaces

```
file: data/raw/violence_spain.csv                      → append-only raw data log
file: data/raw/population_spain.csv                    → INE female population by age/year
file: data/raw/feminicidios_delegacion_{min_year}-{max_year}.json → feminicide reports 2003-2026, one Pydantic-validated report per year (regional/age/origin/relationship/cohabitation breakdowns; report-level source_page/confidence/verified, not per-category — V20); 2003-2005 legacy-format years carry low-confidence stub reports only (see notes field), real 2003-2005 figures still live in `violence_spain.csv` via T1's manual entries (generated by T19/T20)
file: data/raw/sexual_crimes_mir_{year-range}.json    → Informe series (2019-2024) sexual violence extracts from MIR, nested Pydantic-validated schema (categories×sex + nationality×sex per report, no repeated totals per row); generated by T21/T22 (replaces flat CSV — see B8/V19)
file: data/raw/sexual_crimes_mir_anuario_2016-2023.json → Anuario Estadístico series (2016-2023, single file; independently sourced, cross-validated against the Informe series per B23) sexual crimes chapter, same nested schema; generated by T21's `AnuarioParser` (`--mode anuario`); kept in a separately-tagged file rather than merged into the Informe file, since the two are distinct publications (see B23)
file: data/raw/sexual_crimes_mir_balance_2019-2025.json → Balance de Criminalidad quarterly series (2019-2025; 2016-2018 excluded, older PDF table layout not supported by current parser, see B26; independently sourced, cross-validated against the Anuario series per B24) sexual crimes NACIONAL-table total + con-penetración subcategory only (coarser than Anuario/Informe — only 2 subcategories exist in this table); one `MIRReport` per year built from that year's Q4 ("enero a diciembre") cumulative figures, NOT a sum of the 4 quarters (each quarterly PDF is cumulative year-to-date, not incremental — see B24); generated by T55's `BalanceParser`/`run_balance_batch` (`--mode balance`)
file: data/raw/hate_crimes_mir_2016-2021_2023.json     → MIR "Informe de Delitos de Odio" series (2016-2021, 2023; 2022 has no dedicated PDF, see B28), 13-category ámbito breakdown + 3-tier total (TOTAL DELITOS / INFRAC. ADM. / TOTAL DELITOS E INCIDENTES DE ODIO, 2019+); generated by T59's `OdioParser`/`run_odio_batch` (`--mode odio`)
file: data/raw/migration_spain.csv                     → migration flows & stock 2000-2025: by sex, age, origin nationality, foreign/Spanish split (T11, 2337 rows; merged from claude/spain-migration-data-PsIYr 2026-06-07); joint age(5yr)×sex×citizenship cross for Morocco/Algeria (flow 1998-2024, stock 2002-2025) added via `src/parse_eurostat_migration_cohort.py` from Eurostat bulk `migr_imm1ctz`/`migr_pop1ctz` (V25 cohort denominator, real data not extrapolated)
file: data/sources/migracion_espana.md                 → migration source doc — INE EMCR/EVR/Padrón/ECP, Eurostat, MISSM table refs + caveats (2008, 2021 method breaks)
file: data/raw/migrant_crime_numerator.csv             → crime/conviction counts by nationality (Spanish/foreign), offence type, age, sex — extracted from MIR Informes + INE Condenados (generated by T26/T27; T27 group-sexual-violence slice done, 43 rows, general-crime Balance de Criminalidad slice still pending)
file: data/processed/migrant_crime_rates.csv           → absolute per-100k crime rates by nationality × demographic slice, with CIs (generated by T28, planned)
file: data/processed/rates.csv                         → derived incidence rates (generated)
file: data/processed/lifetable.csv                     → cumulative probability output (generated)
file: data/processed/covariate_data.csv                → political & immigration series (generated)
cmd:  `python src/parsers/feminicide_parser.py --pdf <pdf_path> | --pdf-dir <dir>` → parse one or all Delegación feminicide PDFs (2003-2026) → single `data/raw/feminicidios_delegacion_{min_year}-{max_year}.json` (T19,T20)
cmd:  `python src/parsers/mir_parser.py <pdf_dir>`               → extract MIR PDFs (Informe + Anuario) → CSV with validation
cmd:  `python src/parsers/mir_parser.py --mode balance --pdf-dir <dir> [--out-dir <dir>]` → parse Balance de Criminalidad quarterly PDFs' NACIONAL sexual-crimes rows → one `MIRReport`/year from that year's Q4 (annual) figures, NOT summed quarters (T55, see B24)
cmd:  `python src/parsers/mir_parser.py --mode odio --pdf-dir <dir> [--out-dir <dir>]` → parse "Informe de Delitos de Odio" PDFs (content-located table, chart-rendered — word-clustering extraction, not `extract_tables()`) → one `MIRReport`/year, 2022 correctly skipped (no PDF exists, see B28) (T59)
cmd:  `python src/parsers/mir_violence_parser.py <pdf_path>`     → parse "Informe evolución violencia mujer" 2015-2019 PDF (5 tables incl. victim-perp relationship + location of offense), stdout summary only — available, untapped input for T32
cmd:  `python src/parsers/mir_violence_extractor.py <pdf_dir_or_file>` → extract same PDF's 5 tables → `data/raw/mir_violence_sexual_2015-2019.csv`
cmd:  `python src/parsers/mir_migrant_nationality_parser.py`     → extract MIR group-sexual-violence + reference PDFs → `data/raw/migrant_crime_numerator.csv` (T27)
cmd:  `python src/compute_migrant_crime_rates.py`      → numerator ÷ denominator → per-100k rates with CIs (planned, T28)
cmd:  `python src/pipeline.py`                         → raw → processed
cmd:  `python src/lifetable.py`                        → rates → cumulative probs
cmd:  `python src/regression.py`                       → covariate analysis
out:  reports/                                         → final write-ups & charts
html: docs/index.html (3 main tabs: violence, mortality, migration; `violence` nests 3 sub-tabs: sexual-crimes+Macroencuesta, feminicides, others [domestic-violence denuncias, all-cause homicide, EIGE sex/relationship breakdown, victim-perp matrix, homicide rate trend, confidence-distribution chart]) → interactive dashboard with rich visualizations
doc:  `data/PIPELINE.md`                               → script-level companion to this §I: every script under src/ + src/parsers/, its reads/writes, and its §T task
doc:  `data/sources/eige_fra_comparative.md`            → EIGE (Vilnius)/FRA (Vienna)/Eurostat EU-comparative GBV data — Eurostat EU-GBV survey `gbv_*` REST API (2024 wave, confirmed machine-readable, T56), EIGE administrative IPV/DV indicators (non-comparable across countries per EIGE's own caveat), EIGE femicide harmonisation (pilot phase, no operational cross-country count yet), FRA EU-MIDIS II country sheets (T57); enables Spain-vs-EU comparison using standardized methodology, complementing this repo's Spain-only series (macroencuesta/MIR/Delegación)
doc:  `data/sources/mir_delitos_odio.md`                → MIR "Informe de Delitos de Odio" source doc — per-year hand-verified ámbito figures 2016-2023, 2017 discapacidad→diversidad-funcional methodology break, 2022 publication gap, chart-rendered-table extraction method, mixed thousands-separator formatting caveat (T59)
```

---

## §V — Invariants

V1: ∀ row ∈ violence_spain.csv → `row_id`, `violence_type`, `year`, `value`, `unit`, `source_name`, `source_table`, `confidence` ! non-empty.  
V2: `confidence=high` → source is primary govt/official publication, value directly readable (not computed by prior AI).  
V3: `confidence=medium` → source is primary but value requires minor computation (e.g. rate from count + population) or source is reputable secondary.  
V4: `confidence=low` → source is ambiguous, secondary, or value inferred.  
V5: `confidence=unverified` → value originates from prior AI conversation; ⊥ used in modelling until cross-checked.  
V6: ∀ rate computation → denominator ! stated (female population, female 15-49, etc.).  
V7: Definition-break years (2022 LO 10/2022, 2023 reform) ! flagged in `notes` for sexual-offence rows.  
V8: Femicide rows ! distinguish registry: `Delegación_Gobierno` (partner/ex only) vs `INE_MNP` (all female homicide) vs `CGPJ` (judicial).  
V9: ∀ probability estimates → methodology section in report ! describe competing-risks model & assumptions.  
V10: Covariate series ! cover same 2000–2025 range as violence series; gaps flagged.  
V11: Dark-figure multipliers ! sourced from macroencuesta or published academic estimates; ⊥ invented.  
V12: ∀ extracted dataset → parser ! validate: sum(category_breakdown) = reported_total, sum(age_groups) = reported_total, sum(region/origin/demographic breakdowns) reconcile to headline count; validation ⊥ pass until all cross-checks pass. Discrepancies ! logged with row ID + expected vs actual + PDF page reference.  
V13: B6 discrepancy (violaciones series incompatibility, confirmed pre-2019 only per B23) ! always documented in source_document + notes: which legal articles included (Art.179 agresión con penetración only? + Art.181 abuso with penetration?), which MIR source (Anuario headline ~5,200/yr vs Informe subcategory ~1,200/yr), approximate multiplier if known. No silent reconciliation. Applies equally to any other Anuario-vs-Informe category mismatch found later (e.g. B23's 2023 pornografía de menores anomaly).  
V14: ∀ subgroup-relative-rate claim (e.g. "X% of crimes/perpetrators are nationality N") → ! paired with (a) the absolute base rate = count(N) ÷ population(N) per 100k, and (b) the population denominator + its source/year. Relative framings ⊥ published standalone — they obscure that an elevated relative rate can still correspond to a tiny absolute share (e.g. 3% absolute incidence vs 1% is a 3x relative difference but both ⊥ "large"). Directly targets B8-class claims (Haiku "32% extranjeros → 3.43x more likely", row 74 "37.3% foreign perpetrators").  
V15: peligrosity ! reported as bracket [lower = convicted (INE Condenados), upper = identified (detenidos+investigados)] per 100k men of the slice; numerator = distinct persons (deduped), ⊥ crime-event counts (avoids one-perp-many-crimes inflation); denominator = male population of that slice (Padrón/ECP); subject to V14 relative/absolute pairing.  
V16: any conversion between victimization rate and peligrosity ! account for one-aggressor→multiple-victims and repeat victimization via the §E relationship distribution; adjustment factor + source ! documented.  
V17: GBV justice-funnel rates ! keep administrative sources distinct — MIR police denuncias ≠ CGPJ judicial ≠ Fiscalía diligencias ≠ INE condenas; counts ⊥ divided across incompatible sources without a documented bridge.
V18: Hate-speech datasets ! prioritized from established watchdog organizations (e.g., RAXEN, SOS Racismo, CEAR) + primary Interior/Fiscalía sources; ∀ correlation with migration data/crime ! explicitly document definitions, causality limits (associative only), and data limitations (survey coverage/reporting bias). 
- Primary source trace: CEAR report "Los delitos e incidentes de odio, especialmente aquellos relacionados con racismo y xenofobia, aumentaron un 23,6 % en 2025, la mayor cifra desde que hay registros del Ministerio de Interior" ! validated against Ministry of Interior annual hate-crime statistics.
- Victimization/Perpetrator balance: Analysis ! encompass comparative crime rates by different migrant populations, and crimes suffered *by* those populations (including systemic discrimination, institutional harm, and police practices) — using watchdog reports (e.g., reports on police profiling/institutional racism) alongside official statistics.
V19: Generated dataset filenames ! computed from the actual min/max year of the records they contain (e.g. `f"sexual_crimes_mir_{min_year}-{max_year}.csv"`), ⊥ hardcoded aspirational ranges; §I file-path documentation ! kept in sync whenever the real range changes.
V20: ∀ nested-JSON schema → a field lives at the level where its value can legitimately differ between siblings; if a field is structurally constant across every child of a parent (e.g. `source_page`/`source_table`/`verified` — same for every category in one report), it ! live on the parent only, ⊥ duplicated per child.
V21: ∀ multi-input parser run (batch/dir mode) → exactly one output dataset artifact ! written, named by the actual year range covered; ⊥ one-file-per-input plus a redundant consolidated copy of the same data.
V22: ∀ MIRDataset built from a batch/dir run → report years ! unique; ∃ 2 input PDFs inferring the same year → run_batch ! raise (loud failure listing the colliding filenames), ⊥ silently include both in one dataset.
V23: MIR crimes-by-category & crimes-by-nationality tables ! never crossed in the source (confirmed: p.19/p.31 of 2023 Informe give nationality×sex totals only, no per-category split); ⊥ claim any category-specific (e.g. rape-specific) nationality rate from MIR alone — requires a source with that cross (e.g. INE Condenados, T26 secondary check).
V24: ∀ 2019-2024 sexual-crime category time-series comparison → pre-LO-10/2022 "abuso sexual" + "agresión sexual" ! summed into one unified "agresión sexual" series (and likewise the con-penetración pair) before comparison; raw pre/post labels ⊥ compared directly (extends C3/V7).
V25: recent-arrival-cohort population (no direct tenure-stratified table exists) ! approximated as cumulative net migration inflow over the trailing ~3yr window, joined against `migration_spain.csv` (T11) age-band × sex × origin-nationality data (closest available band to 15-59 male); settled_pop = stock − cohort_pop; ⊥ presented without documenting the approximation + its caveats (naturalization, re-emigration, irregular-migration undercount all bias in either direction). UPDATE 2026-07-01: for Morocco (MA) and Algeria (DZ) specifically, `migration_spain.csv` now carries the *real* joint 5yr-age-band × sex × citizenship cross (flow 1998-2024, stock 2002-2025, via Eurostat `migr_imm1ctz`/`migr_pop1ctz` bulk — INE's own tables 24290/36825 only reach 2021/2022) — no extrapolation needed for T41's cohort_pop/settled_pop split for these two countries; the cumulative-inflow approximation above still applies wherever this direct cross is absent (other nationalities in T42's broader correlation scope).  
V26: SPEC.md ! contain exactly one `## §T` section header and one §T pipe table; amendments to §T ! edit the existing table in place, ⊥ append a new section/table.  
V27: ∀ §T task id ! unique across the file; ⊥ two rows share one id.  
V28: §T row prose ! stay task-description length (what/where/status); once a task's results/analysis grow past that, they ! move to `reports/<name>.md` with the row keeping a 2-3 sentence summary + link.  
V29: ∀ §T row → `track` column ∈ {A,B,C,D,E,F,G,infra} (comma-separated if cross-cutting), matching §G's lettered Key-Quantities list.
V33: ∀ script ∈ `src/` ∪ `src/parsers/` → row ! exist in `data/PIPELINE.md` naming its §T task; row with no §T task, or script with no row, = orphan by definition and ! resolved (task added or script archived) before considered done.
V34: before any file/script is deleted or archived as "superseded" → target report/PDF/data coverage of the candidate ! checked against the file(s) claimed to supersede it, not the import graph alone; import-graph-only "unreferenced" is insufficient evidence of dead code.
V35: PDF text extraction, CSV row writing, and Spanish-number parsing in `src/parsers/` ! go through `src/parsers/utils.py`, ⊥ reimplemented per-file.
V36: ∀ Spain-vs-other-EU-country comparison using EIGE/FRA/Eurostat GBV data → ! carry explicit caveats: (a) EIGE `genvio_int_adm_ipv` administrative indicators are ⊥ cross-country comparable per EIGE's own stated caveat (differing counting rules) — Spain-only confirmation data, not a comparator; (b) self-report prevalence differences (e.g. Sweden 48.2% vs Spain 28.6-30% lifetime IPV) reflect disclosure/normalization differences ("Nordic paradox"), not necessarily true-incidence differences — ⊥ read as "Spain does better"; (c) 2014 FRA survey vs 2024 EU-GBV survey is a methodology/question-redesign break, not a real trend — ⊥ chained into one time series (extends C3/V7's national-level definition-break rule to EU-survey wave changes).

---

## §T — Tasks

### Roadmap
- **Phase 0 — housekeeping/now**: sync T11/T-mig-tab status; rewrite README + mermaid (change #5); create `reports/results.md` (change #2); expand §G/§C/§V (changes #1, #3).
- **Phase 1 — finish core violence series** (unblocks life table): T2, T21, T22, T4, T5, T14, **T35** (literature synthesis → dark-figure & relationship priors).
- **Phase 2 — core probability deliverable** *(current priority)*: T8, T9, T24, **T15** (now incl. extraction map + composition DAG), T16.
- **Phase 3 — peligrosity & relationship**: **T30, T31** (peligrosity engine + bracket), **T32, T33** (relationship structure + adjustment); nationality slice = T26, T27, T28, T29.
- **Phase 4 — covariate regression & projections**: T10, T12, T13.
- **Phase 5 — later expansions**: **T34** (GBV non-sexual funnel), **T36** (docs composition/methodology diagram), peligrosity for other petty crimes, **T54** (analysis-script consolidation backlog), **T56/T57** (EU-comparative GBV data — Eurostat gbv_* API + EU-MIDIS II Spain extraction), **T58** (EIGE administrative-indicator pull, backlog).
- **Expansion threads (N1–N19)** *(added 2026-07, mirrored 1:1 in Project House milestones M1–M8)*: data-intake widening (N1–N6: INE, CIS concern series, migrant outflows, undocumented-stock estimates, political/TV discourse corpus, MENA/asylum sources); transversal dark-figure/reporting-rate layer (N7 policy annotation, N8 reporting-rate drift, N9 correction layer); migration-denominator rigour (N10 net-stock, N11 undocumented sensitivity); discourse & perception (N12 salience, N13 perception-vs-phenomenology); EU comparison (N14 analysis, N15 dashboard); MENA/asylum (N16 fiscal cost, N17 crime attribution); asylum acceptance-rate deep-dive (N19 data, N18 Spain-vs-IT/DE analysis + downstream effects).

| id | status | track | task | cites |
|---|---|---|---|---|
| T1 | x | A,B | Populate `violence_spain.csv` — femicide (partner/ex): Delegación del Gobierno 2003–2024; rows 1–22,90,91,104,105 complete; 2024=47 verified | V1,V2,V8 |
| T2 | ~ | A,B | Populate `violence_spain.csv` — all-cause homicide by sex: EIGE 2022 (rows 107–113), INE ECM 2023 rates+counts (rows 114–121), MIR Informe perp-sex breakdown (rows 122–124), feminicidio.net (125–126). INE ECM 2000–2024 full series now in `data/processed/mortality_spain_ine_ecm.csv` via T49. PENDING: MIR Anuario tables with sex breakdown. Source file: `data/sources/homicidio_espana.md` | V1,V2,V8 |
| T3 | ~ | A,B | Populate `violence_spain.csv` — sexual crimes MIR: 2022–2024 verified (rows 46–56,82–89); 2017–2021 medium; 2000–2016 unverified. BLOCKER: two incompatible violaciones series unresolved (B6) | V1,V2,V7 |
| T4 | . | A,B | Populate `violence_spain.csv` — non-sexual domestic violence denuncias: Ministerio del Interior / CGPJ 2000–2024 | V1,V2 |
| T5 | ~ | A,B | Populate `violence_spain.csv` — Macroencuesta: 2015 & 2019 rows done (medium); 2024 wave rows 94–99 done (high; published 3 Dec 2025); 2011 & methodology-change caveats pending | V1,V3,V11 |
| T6 | x | A,B | Female (& male, total) population by 5-yr age group & year 2000–2025 (mid-year July 1) from INE table 56934 (Estimaciones de Población Actual) → `data/processed/population_spain_midyear_5yr.csv`. Source: 56934 also gives Jan/Apr/Jul/Oct 1971–2025 in `population_spain_estimates.csv`. Cross-check 2024 female: bin sum 24,881,624 = INE all-ages exact match. Script: `src/parse_ine_population.py` | V6,V10 |
| T7 | x | A,B | Age-specific annual incidence rates per 100k → `data/processed/mortality_rates.csv` (198k rows, full deaths × pop join). Plus subsets: `mortality_rates_key.csv` (key causes), `mortality_rates_all_cause_by_age.csv` (concise). Script: `src/compute_mortality_rates.py` | V3,V6 |
| T8 | . | A | Build competing-risks life-table → `data/processed/lifetable.csv` — 1-yr, 5-yr, lifetime cumulative P for 2000-born cohort | V7,V9 |
| T49 | x | infra | All-cause mortality by age × sex × cause 2000–2024 from INE ECM table 7947 → `data/processed/mortality_spain_ine_ecm.csv` (198k rows) + summary CSVs. Source doc: `data/sources/ine_causas_muerte.md`. Scripts: `src/parse_ine_mortality.py`, `src/summarize_mortality.py` | V1,V2,V6 |
| T9 | . | A | Dark-figure estimation: cross-validate police counts vs macroencuesta; compute multipliers per violence type | V11 |
| T10 | . | C | Collect covariate series: far-right vote share (Vox/PP far-right component) per year from CIS / electoral results | V10,C8 |
| T11 | ~ | C,infra | Populate `data/raw/migration_spain.csv` (2337 rows) — flows 2000-2024, foreign-nationality stock 2000-2025, top-10 origin nationalities for 2008-2024 intake & 2025 stock (sex-split sums match published totals exactly 2008-2024), broad age bands 2008-2024 + granular 5-yr bands 2024, SS-affiliation snapshots; EXTENDED 2026-07-01: joint age(5yr)×sex×citizenship cross for Morocco/Algeria (flow 1998-2024, stock 2002-2025) via `src/parse_eurostat_migration_cohort.py` (Eurostat `migr_imm1ctz`/`migr_pop1ctz` bulk, cross-validated exact match against pre-existing MA totals 2022-2024). See `data/sources/migracion_espana.md` | V10,C8,V25 |
| T12 | . | C | Covariate regression: multivariate OLS + BSTS on violence-rate ~ covariates; report associations not causal claims | C8,V9 |
| T13 | . | C | Scenario projections: vary covariates ±10/20%, recompute expected rates | C8 |
| T14 | . | A | Re-verify all `confidence=unverified` rows from prior AI conversation against primary sources | C9,V5 |
| T15 | . | infra | Write `reports/methodology.md` — definitions, legal changes, dark-figure approach, model spec; per-source extraction table + composition DAG (mermaid) + peligrosity/relationship/funnel definitions | V9,C3,C4 |
| T16 | . | infra | Write `reports/results.md` — probability estimates + CIs + scenario table | V9 |
| T17 | x | infra | Restructure dashboard with tabbed interface — refactor `docs/index.html`: tab 1 = sexual crimes + Macroencuesta surveys, tab 2 = feminicides; maintain embedded source hyperlinks; add data-tab attribute to charts | I.*,V8 |
| T18 | x | infra | Download reference documents — fetch 4 additional synthesis reports from FURTHER_SOURCES.md (MIR 2022/2023/2024 methodological reports, CCOO analysis, group-violence report, MTAS 2015–2019 overview); organize by type in `data/sources/` | C1 |
| T19 | x | infra | Build feminicide PDF parser with integrated validation — `src/parsers/feminicide_parser.py` (consolidated, single CLI, `--pdf`/`--pdf-dir`) parses Delegación del Gobierno "Estadística de víctimas mortales por violencia de pareja" PDFs 2003-2026 (24 files); 2006-2026 use the "numbered-table" format (Tabla 2.1-2.4, 3.1-3.4) — fully extracted: per-region, per-age (victim/perpetrator), country of origin (national/foreign), relationship type + cohabitation (Tables 2.1-2.4), prior complaint/process continuation, protective measures requested+adopted, restraining order breach, perpetrator suicide attempt (Tables 3.1-3.4); 2003-2005 use a structurally different legacy "ficha resumen" layout — parser detects this format and emits a low-confidence stub report only (year + source, `notes` points to the manually curated `data/sources/delegacion_gobierno_femicidio.md` figure), full extraction of that era out of scope; **validation gate**: sum(region) = total_victims held exactly for all 21 modern-format years (2006-2026) | C1,C13,V12 |
| T20 | x | infra | Structure feminicide dataset — output validated extracted data as nested JSON (not flat CSV, to avoid repeating report-level totals per category row, per V20) to a single consolidated `data/raw/feminicidios_delegacion_2003-2026.json`; one Pydantic-validated `FeminicideReport` per year (`regional`/`relationship_type`/`cohabitation`/`prior_complaint`/`protective_measures`/`restraining_order_breach`/`perpetrator_suicide_attempt`: `[CategoryCount]`; `age`/`origin`: `[VictimPerpCategoryCount]`; report-level-only `source_document`/`source_page`/`confidence`/`verified`/`notes`) inside a root `FeminicideDataset` model, mirroring `mir_parser.py`'s `MIRDataset` pattern | V1,V2,V6,V12,V19,V20,V21,V22 |
| T21 | ~ | infra | Build sexual violence MIR parser with integrated validation — Informe series DONE (2019-2024, `InformeParser`, tested); Anuario series PARTIAL (`AnuarioParser`, TABLA 3-1-5/3-1-6 text-parse, tested against 2021-2023 PDFs — the only Anuario editions available; 2000-2020 gap remains, no PDFs sourced yet). **validation gate**: sum(categories) = total (enforced in `AnuarioParser._validate()`); B6 discrepancy reconciled for 2021-2023 — direct parse shows Anuario/Informe agree exactly on totals and Art.179-equivalent subcategory (see B23); 2023 pornografía-de-menores cross-publication anomaly documented, not silently fixed | C1,C13,C14,V7,V12,V13 |
| T22 | ~ | infra | Structure sexual violence dataset — output validated extracted data as nested JSON (not flat CSV, to avoid repeating report-level totals per category row) to consolidated per-series files: `sexual_crimes_mir_{min_year}-{max_year}.json` (Informe series, DONE, 2019-2024, tested + regenerated) and `sexual_crimes_mir_anuario_{min_year}-{max_year}.json` (Anuario series, DONE for 2021-2023, tagged separately per B23 rather than merged — two independent publications); one Pydantic-validated `MIRReport` per year (`categories: [CategorySexBreakdown]` for category×sex, `nationality: {victims: NationalityBreakdown, perpetrators: NationalityBreakdown}` at report-total level, report-level-only `source_document`/`source_table`/`source_page`/`verified` (bool, defaults False until manually confirmed against the source PDF), `notes`) inside a root `MIRDataset` model; REMAINING: enrich existing `violence_spain.csv` rows 46–99 from the JSON | V1,V2,V7,C3,V13,V19,V20,V21,V22 |
| T23 | x | infra | Add rich visualizations for extracted data — feminicides tab: stacked bar chart (age groups x-axis, origin breakdown per bar: víctimas españa + víctimas otro país); no rate subtitles (rates deferred to T24). Extend other tabs with existing visualizations | I.*,V1,V6 |
| T24 | ~ | A | Integrate INE population data — use `data/processed/population_spain_midyear_5yr.csv` (by age/sex) + estimate foreign-national population by origin/year; compute per-origin feminicide rates (2024 count ÷ population); add rate overlay to T23 visualization with 95% CIs | V6,C10 |
| T25 | x | infra | Restructure markdown docs — archive stale Haiku phase-log artifacts (`FASE_2_PLAN_2015_2020`, `FASE_3_PLAN_2000_2014`, `FASE_2_PROGRESO_TAREAS_2_1_2_3`, `FASE_2_TAREAS_2.7_2.8_CIERRE_FINAL`, `INFORME_PROGRESO_FASE1_COMPILACION`, `INFORME_PROGRESO_FASE2_TAREA2.1`, `INDICE_MAESTRO`) + `FURTHER_SOURCES.md` to `archive/`; merge `SEXUAL_VIOLENCE_VERIFICATION.md`+`QUICK_VERIFY_SEXUAL_VIOLENCE.md`+`MORTALITY_MIGRATION_VERIFICATION_GUIDE.md` → one `VERIFICATION_GUIDE.md`; add `data/sources/SOURCES_INDEX.md` (1-page index over the 10 living source docs: scope, confidence, known issues) | — |
| T26 | x | D | Extract perpetrator-nationality data (sexual violence) — DONE: `parse_country_breakdown_table`/`CountryBreakdown` in `mir_parser.py` (TDD, `tests/test_mir_parser_nationality.py`, 11 tests green) extracts every region/country row for both victims & perpetrators across all 5 years (2019,2021,2022,2023,2024), incl. 2024's reversed sex-column order and no-sex-breakdown perpetrators table; wired into `InformeParser._extract_nationality` + `NationalityBreakdown.by_country`; regenerated `sexual_crimes_mir_2019-2024.json`, spot-checked (2023 perp Marruecos: male 975/female 26/total 1001/pct 7.3 ✓). REMAINING: replace flagged row 74 (37.3% foreign, confidence=medium) using this data; cross-check INE Condenados table 28716 for category-specific figures (out of scope this phase) | C1,C13,V1,V2,V12,V14,V23 |
| T27 | ~ | D | Extract perpetrator-nationality data (general crime) — MIR Balance de Criminalidad / Anuario "detenidos/investigados por nacionalidad" totals (broader than sexual violence only) → headline absolute-rate numerator, appended to `data/raw/migrant_crime_numerator.csv`. PARTIAL: sexual-violence-adjacent group data done via `src/parsers/mir_migrant_nationality_parser.py` (43 rows, `migrant_crime_numerator.csv`); broader general-crime Balance de Criminalidad/Anuario numbers per original scope still pending | C1,V1,V2,V12,V14 |
| T28 | . | D | Compute absolute migrant crime rates — `src/compute_migrant_crime_rates.py`: join numerator (T26/T27, crime counts by nationality) ÷ denominator (`migration_spain.csv` stock by nationality/origin/age/sex, supplement with INE table 36825 if finer joins needed) → per-100k rates with binomial/Poisson CIs, by nationality and by demographic slice (origin region × age × sex) → `data/processed/migrant_crime_rates.csv` | V6,V14,C10 |
| T29 | . | D | Dashboard/report — present absolute migrant-crime-rate analysis pairing relative and absolute framings side-by-side (e.g. "37% of detentions ↔ N per 100k residents vs M per 100k for nationals — both small in absolute terms"); add dashboard section + `reports/migration_crime.md` write-up addressing the "migrants commit more crime" generalization directly with sourced absolute base rates by demographic slice | I.*,V14,C8 |
| T30 | . | D | Extract distinct-perpetrator counts (sexual) — MIR Informe detenidos+ investigados 2019–2024 (deduped where possible) + INE Condenados table 28716 by offence/sex/age/nationality → `data/raw/perpetrators_sexual.csv` (numerator for peligrosity bracket; generalizes T26). | C1,C13,V1,V12,V14,V15 |
| T31 | . | D | Compute peligrosity → `src/compute_peligrosity.py`: distinct perps ÷ male population (Padrón/ECP) → bracket [convicted, identified] per 100k men, by age × nationality × origin, with CIs → `data/processed/peligrosity_rates.csv`; nationality slice ! reconcile with `migrant_crime_rates.csv` (T28). | V6,V14,V15,C10 |
| T32 | . | E | Extract victim–aggressor relationship structure — Macroencuesta (partner / known / stranger; ±penetration), MIR victim-perp matrix (2010–2012 + any newer), Delegación `relationship_status` → `data/processed/relationship_structure.csv` + victims-per-aggressor & repeat-victimization factors. | C5,V16 |
| T33 | . | E | Apply relationship adjustment — adjust §A victim risk & §D peligrosity using T32 factors; document each adjustment + source. | V16,V9 |
| T34 | . | G | *(later)* GBV non-sexual justice funnel — extract denuncias (CGPJ/MIR), diligencias (Fiscalía Memorias), condenas (INE Condenados/CGPJ), protection orders; compute reporting rate (vs Macroencuesta physical/psychological prevalence), prosecution rate, conviction rate → `data/raw/gbv_funnel.csv` + `data/processed/gbv_funnel_rates.csv`. | C5,V11,V17 |
| T35 | . | infra | Literature-evidence synthesis — from `fuentes_secundarias_analisis_espana.md` (29 sources) + 4 reference PDFs, extract each study's headline metrics + trace to primary source → `data/sources/literature_evidence.md` table; supplies dark-figure multipliers, victim-perp matrices, relationship priors as cross-checks. | C1,C15,V11 |
| T36 | . | infra | *(later)* Add composition/methodology diagram to `docs/index.html` (dashboard rendering of the extraction→metric DAG). | I.* |
| T37 | . | F | Collect hate-speech-watchdog datasets (RAXEN, SOS Racismo) & primary source data (Interior/Fiscalía/INE) on hate-crimes/incidents (racism/xenophobia 2025 spike). | V18 |
| T38 | . | C,F | Map correlation series: migration rates (T11), public polling (CIS worry), xenophobic party voting shares (CIS/Interior). | V18,C8 |
| T39 | . | F | Analyze media presence of hate-speech/xenophobia: literature review/content analysis (if available via watchdog reports). | V18 |
| T40 | . | F | Write `reports/hate_speech_analysis.md` — synthesize hate-speech findings, correlate with migration/crime, address associations. | V18 |
| T-mig-tab | x | infra | Rebuild migration dashboard tab on current `docs/index.html` architecture (T17/T23 tabbed architecture) — charts G1 (inflow + 2008 break), G2 (origin composition), G3 (sex split), G4/G5 (age composition/profile), G6 (stock); delivered via `src/build_migration_dashboard_data.py` (commit `55cfa64`). | I.*,V1,V6 |
| T41 | x | D,C | Test Morocco/N.Africa post-2022-cohort tenure hypothesis, extended to South America/EU-Europe (share only) — DONE: `src/analyze_cohort_crime_rate.py`, five formal hypothesis tests (A: period rate-ratio, B: cohort-specific decomposition, C: share-of-total, D: Test A per-100k, E: diff-in-diff vs Spanish population). Headline: Marruecos/Argelia total-group rates significantly elevated post-2022 (Test A), but the rise is NOT concentrated in the newly-arrived cohort specifically (Test B); vs the Spanish population's own rising baseline, Marruecos's rise is significantly smaller and Argelia's significantly larger in most comparisons (Test E). South America's crime share rose significantly from 2023 on; EU-Europe's fell by 2024 (Test C only — no population time series exists yet for these groups, see T43-T48). Full methodology, all RESULT figures, and every caveat/UPDATE: `reports/cohort_tenure_analysis.md`. | V6,V9,V14,V15,V25,C8,C10 |
| T42 | x | B,D | Sexual-crime category & nationality trend analysis — DONE: `src/plot_sexual_crime_trends.py`. Headline: both unified crime-category series rose 2019→2024; foreign-perpetrator counts grew faster than Spanish (+65% vs +39%); per-country crime counts correlate strongly with cumulative migration inflow (r=0.98-0.99, MA/DZ/CO/RO, n≤5 points, descriptive only). Full results, per-country caveats, correlation figures: `reports/nationality_crime_trends.md`. | V10,V14,C8,V24,V25 |
| T43 | . | D | Obtain real age×sex×citizenship joint-cross migration/stock data — South America. Extend `src/parse_eurostat_migration_cohort.py`'s `CITIZENS` list beyond `["MA","DZ"]` to add CO, EC, PE, VE, BO, PY, AR (Colombia, Ecuador, Peru, Venezuela, Bolivia, Paraguay, Argentina — the T41 South America group; MIR name-instability caveats from T42 apply when mapping names→codes); re-run against the Eurostat migr_imm1ctz/migr_pop1ctz bulk TSVs (must be manually downloaded — not present in `data/raw/`) to get the real 5yr-age-band × sex × citizenship joint cross per country; append to `migration_spain.csv`. Blocked until the bulk TSVs are obtained. ⊥ approximate the population time series from a single-year snapshot (rejected as not defensible, see T41's report). | V6,V25,C10 |
| T44 | . | D | Obtain real age×sex×citizenship joint-cross migration/stock data — EU-Europe excl. Spain. Extend `src/parse_eurostat_migration_cohort.py`'s `CITIZENS` list to add RO, IT, DE, FR, BG, PT, PL, BE, NL (Rumania, Italia, Alemania, Francia, Bulgaria, Portugal, Polonia, Belgica, Holanda — the T41 EU-Europe group); same Eurostat bulk-TSV source/process as T43 (can be run together in one script invocation once files are downloaded); append to `migration_spain.csv`. Blocked until the bulk TSVs are obtained. ⊥ approximate the population time series from a single-year snapshot. | V6,V25,C10 |
| T45 | . | D | Run Test A (T41 methodology: period-level two-sample Poisson rate-ratio z-test, whole-group crime-count ÷ male-15-59-stock rate, pre-2022 vs post-surge years) for the South America and EU-Europe groups once T43/T44 land real population data — re-run `src/analyze_cohort_crime_rate.py`'s Test A runner (already generic over group/code/stock/crime dicts, no code change needed) per new group; extend `cohort_tenure_period_test.csv` + `cohort_tenure_rate_ratio.png`. Depends on T43, T44. | V6,V9,V14,C8 |
| T46 | . | D | Run Test B (T41 methodology: cohort-specific decomposition — crime(year) = r_settled_baseline·settled_pop + r_cohort·cohort_pop, isolates whether newly-arrived sub-population specifically is elevated vs settled) for the South America and EU-Europe groups once T43/T44 land real population data — re-run Test B runner per new group; extend `cohort_tenure_rates.csv` + chart. Depends on T43, T44. | V6,V9,V14,V25,C8 |
| T47 | . | D | Run Test D (T41 methodology: Test A restated per 100,000 males 15-59, presentational unit conversion only, same statistic/p-values) for the South America and EU-Europe groups — trivial once T45 lands (adds `rate_baseline_per_100k`/`rate_test_per_100k` columns to the Test A CSV for the new groups). Depends on T45. | V6,V14,C10 |
| T48 | . | D | Run Test E (T41 methodology: diff-in-differences / log-linear Poisson interaction z-test — is the group's rate ratio significantly different from the Spanish male 15-59 population's rate ratio over the same periods) for the South America and EU-Europe groups once T43/T44 land real population data — re-run Test E runner per new group; extend `cohort_vs_spanish_test.csv` + chart. Depends on T43, T44. | V6,V9,V14,V15,C8 |
| T50 | x | C,D | MIR reports × migration flux correlation analysis — DONE: `src/analyze_mir_migration_correlation.py`. Examines whether sexual-crime growth/foreign-perpetrator share outpaced migration growth 2015-2024, and whether Spanish/foreign per-100k perpetrator rates converged or diverged; output `data/processed/mir_migration_rates.csv`. Discovered as an unwired orphan during the T53 parser-consolidation cleanup — code and output predate this task id. | V14,V25,C8 |
| T51 | x | B,D | Rape trend & nationality analysis — DONE: `src/analyze_rape_trend_nationality.py`. Answers: year-by-year reported-rape trend to 2024; convicted-sex-offender nationality distribution 2017-2024 (via `parse_ine_tabla28716.py`'s table-28716 extract); whether the "immigration drives the increase" claim holds against known-vs-unknown-perpetrator reporting-rate context. Stdout report only, no file output. Discovered as an unwired orphan during the T53 parser-consolidation cleanup. | V14,C1 |
| T52 | x | A,E | Aggression profile analysis — DONE: `src/analyze_aggression_profile.py`. Extracts four dimensions from `violence_spain.csv`: unreported-case (cifra oculta) rates by aggression type, known-vs-unknown perpetrators, context (relationship + location), single-vs-multiple perpetrators; writes `data/processed/aggression_profile_summary.txt`. Discovered as an unwired orphan during the T53 parser-consolidation cleanup. | V11,C5 |
| T53 | x | infra | Consolidate `src/parsers/` shared logic — DONE: created `src/parsers/utils.py` (`extract_text`, `write_csv_rows`, `parse_es_number`, `cli_require_arg`); migrated all 6 parser files to import from it, removing inlined pdftotext/CSV-writer/argv-guard duplicates; `mir_violence_parser.py`'s `to_num()` now delegates to `parse_es_number()` (real bug fix — old version silently dropped the fractional part of any decimal-comma value); brought `feminicide_extractor.py`/`mir_violence_extractor.py`/`mir_violence_parser.py` docstrings up to the standard of the other 3 files. Verified per-file via `git diff HEAD` (mechanical-only) + live re-run against source PDFs/data. Also produced `data/PIPELINE.md` (this task's script-map deliverable). | V35 |
| T54 | . | infra | *(later, backlog)* Consolidate near-duplicate analysis-script patterns — `compute_feminicide_rates.py`/`compute_mortality_rates.py` share an identical count÷population×100k-with-CI pattern (different sources); `build_dashboard_data.py`/`build_migration_dashboard_data.py` share an identical stdout-JS-emission pattern for `docs/index.html`. Documentation-only this round (see `data/PIPELINE.md`); code consolidation deferred. | — |
| T55 | x | infra | Parse MIR "Balance de Criminalidad" quarterly sexual-crimes rows (40 PDFs staged, 2016 Q1-2026 Q1) — DONE: `BalanceParser`/`run_balance_batch` in `mir_parser.py` (`--mode balance`), extracts the NACIONAL table's row 5 (total)/5.1 (con penetración)/5.2 (resto, residual bucket) via `pdfplumber` page-text + regex (NOT `extract_tables()` — unreliable); NACIONAL page located by front+back 6-page scan (position not fixed, varies by edition); header year-column count varies 2 vs 3 cols (some editions add a pre-pandemic 2019 baseline) — target column matched by year value, not fixed index; footnote text ("tipificación previa a LO 10/22") pollutes naive left-to-right numeric-token indexing — count columns read from the right, immediately before the comma-bearing Var.% columns. CRITICAL finding: each quarterly PDF is CUMULATIVE year-to-date ("Acumulado enero a marzo/junio/septiembre/diciembre") — summing 4 quarters over-counts the true annual total by ~2.4-2.6×; correct annual total = the Q4 report's own figure, not a sum (see B24). Q4 totals cross-validated against Anuario/Informe (T21): 2019-2021 near-exact/exact match (within 0.5%); 2022-2024 diverge 7-9% on the total (Balance pages self-flag "Datos pendientes de consolidar" — provisional). 2016-2018 use an older, unsupported table layout — `BalanceParser` correctly detects and skips them rather than emitting wrong data (see B26). 2026 Q1-only (no Q4 yet) correctly skipped. Output: `data/raw/sexual_crimes_mir_balance_2019-2025.json` (7 reports). Documented in `data/sources/mir_informes_delitos_sexuales.md` and `SOURCES_INDEX.md`. | C13,C14,V12,V13,V19,V21,V22,V35 |
| T56 | . | C,infra | Pull Eurostat EU-GBV survey `gbv_*` aggregated-indicator API (2024 wave, joint Eurostat/FRA/EIGE) for ES + comparators (FR, DE, IT, SE, EU27_2020) — confirmed working REST JSON pull, no PDF parser needed (`https://ec.europa.eu/eurostat/api/dissemination/statistics/1.0/data/gbv_ipv_type?format=JSON&lang=en`; sibling tables per `gbv_sims` metadata cover non-partner violence, workplace sexual harassment, stalking, childhood violence). First genuinely cross-country-comparable Spain-vs-EU prevalence table this repo has. Must first pin the exact table/dimension filter for Spain's lifetime-IPV figure — two pulls in the research pass returned 28.6% vs 30.0% for the same nominal cell (`data/sources/eige_fra_comparative.md` §2.3/Open-followups) — before citing a headline number. Subject to V36's comparability caveats. → `data/raw/eu_gbv_survey_{countries}.csv` | C1,C15,V14,V36 |
| T57 | . | F | Extract EU-MIDIS II Spain country-sheet PDF (`fra-2019-eu-midis-ii-summary-results-country-sheet-spain_en.pdf`, URL confirmed live, plain WebFetch could not decode it — needs `pdftotext`/pdf skill) — resolves the specific violence/harassment-reporting-rate breakdown (88%/90%/72%, currently EU-aggregate only, `discurso_odio_inmigracion_espana.md` §2.3bis) to a Spain-specific figure; cross-check against REIC academic paper (Bermejo et al. 2021, *Revista Española de Investigación Criminológica* vol.19, Spain subsample N=1,563) which already yields 3 Spain-specific discrimination/profiling figures (35% antigypsism 5yr recall vs 27% EU avg; 21% perceived ethnic-profiling stops vs 8% EU avg; 12% reporting rate) at medium confidence pending primary-PDF cross-check. See `data/sources/eige_fra_comparative.md` §3 | C1,C15,V18 |
| T58 | . | C,infra | *(later, backlog)* Pull EIGE `genvio_int_adm_ipv` 13-indicator IPV/DV family for Spain + comparator countries, and the Gender Equality Index violence-domain composite scores (`dgs-p.eige.europa.eu/data/view?code=...`) for all EU-27 — EIGE's own documentation says the IPV/DV administrative indicators are explicitly NOT cross-country comparable (differing counting rules); pull anyway as a negative/caveat-establishing result quantifying how large that gap is in practice. Also resolves the open question of whether Spain is among the 12 EU countries with a calculated violence-domain composite score. Subject to V36(a). | C1,C15,V36 |
| T59 | x | C,infra | Parse MIR "Informe de Delitos de Odio" (hate crime) PDFs (7 staged, 2016-2021+2023; no 2022 edition, see B28) — DONE: `OdioParser`/`run_odio_batch` in `mir_parser.py` (`--mode odio`), locates the target table by content match (`HECHOS CONOCIDOS REGISTRADOS`+`RACISMO`, page position varies 2016:p14→2023:p12) since the table is chart/infographic-rendered (`pdftotext -layout`/`pdfplumber.extract_tables()` both fail on it) — extracted via `pdfplumber.extract_words()` Y-position row-clustering (3pt tolerance); reads numeric columns left-to-right (opposite of `BalanceParser`, different footnote-noise pattern); dynamically detects 2-or-3 year-columns per edition from header tokens; 3-tier total structure from 2019 (TOTAL DELITOS = ámbito sum; + INFRACCIONES ADM. = TOTAL DELITOS E INCIDENTES DE ODIO, the real headline); 13 ámbito categories normalized across mid-series renames (2017 discapacidad→diversidad-funcional methodology break, see B28) and additions (antigitanismo 2019+, discriminación generacional/enfermedad 2018+); V22 year-collision guard + B25-class zero-record guard reused from `BalanceParser`. 32/32 tests pass (`tests/test_mir_parser_odio.py`); full repo suite 50/50, no regression. Output: `data/raw/hate_crimes_mir_2016-2021_2023.json` (7 reports; filename honestly shows the 2022 gap, not a misleading contiguous range, per V19). Documented in `data/sources/mir_delitos_odio.md`. | V12,V19,V22,C13 |
| N1 | . | infra | Broaden INE table coverage — pull additional INE tables (Condenados t.28716 further years/offence detail, MIR/INE homicide sex-breakdown, other denominator/offence tables) into `data/raw`; fetch+parse only, downstream analysis in the consuming task. | C1,V33 |
| N2 | . | C,F | Acquire CIS "principales problemas del país" barometer concern-ranking series (top national concerns, 1st–4th place, 2000–2025); isolate immigration/crime/GBV-relevant categories; input to N13's perception analysis. | V18,C8 |
| N3 | . | C,infra | Acquire migrant outflow/emigration data (by nationality, age, sex) so net stock (inflow−outflow) can be computed for proper rate denominators (N10); document 2008/2021 method breaks; joinable to `migration_spain.csv`. | V10,C8,V25 |
| N4 | . | C,D | Source/estimate irregular (undocumented) migrant population stock (visa overstayers, unauthorized entries) + uncertainty; input to the undocumented-migration sensitivity bound (N11); trace the "minor vs legal migration" claim to primary sources. | V25,C10 |
| N5 | . | F | Acquire longitudinal political + mainstream-TV discourse dataset on immigration/crime/GBV topic salience, channel-separable (politicians vs broadcasters); structure only, analysis in N12. | V18 |
| N6 | . | D,F | Acquire MENA/asylum source data — (a) national+regional budget lines for unaccompanied-minor & asylum-seeker support; (b) criminal-case counts + offence types attributed to these populations, with population denominators; feeds N16/N17. | C1,V14 |
| N7 | . | A,B | Policy-change annotation layer — catalogue major Spanish GBV/sexual-offence legal changes 2000–2025 (LO 1/2004, LO 10/2022, 2023 reform) and annotate the reported series for before/after policy-effect reading, without conflating definition breaks (C3/V7) with real change. | C3,V7 |
| N8 | . | A | Reporting-rate time series & drift model — estimate how reporting propensity (not incidence) changed 2000–2025 (post-#MeToo disclosure shifts) per violence type; separate reporting-driven from incidence-driven trend components; transversal input to N9/§A/§C. | V11,C5 |
| N9 | . | A | Transversal correction layer — apply dark-figure multipliers (T9) + reporting-rate drift (N8) to each reported violence type → reported↔corrected incidence brackets feeding the life table (T8) and covariate analysis; every step documents its multiplier+source. | V11,C2 |
| N10 | . | C,D | Net-stock denominators — using outflow data (N3), recompute migrant stock as net (inflow−outflow) rather than inflow-only as the denominator for migrant peligrosity/victimization rates; quantify the difference vs inflow-only. | V6,V25,C10 |
| N11 | . | C,D | Undocumented-migration sensitivity — using N4 estimates, bound how irregular migration biases the numerator and denominator of migrant crime/victimization rates (both directions) and whether it materially moves headline estimates. | V25,C10,V14 |
| N12 | . | F | Longitudinal discourse-salience analysis — from N5's corpus, quantify net/percentage salience of immigration/crime/GBV topics over time, politicians vs mainstream-TV channels separately. | V18 |
| N13 | . | C,F | Perception vs phenomenology correlation — correlate real phenomenology (hate-crime/crime data) × discourse salience (N12) × public perception (CIS top-concern ranking, N2) over time; separate perceived from actual salience, examine lead/lag; associative only. | V18,C8 |
| N14 | . | A,F | Spain-vs-EU comparison analysis — from the EU-GBV pulls (T56/T57/T58) build the comparable Spain-vs-EU prevalence table (ES vs FR/DE/IT/SE/EU27) with V36 caveats (non-comparable EIGE admin indicators, Nordic-paradox disclosure gap, 2014-FRA-vs-2024-EU-GBV wave break). | V36,C15,V14 |
| N15 | . | infra | EU-comparative dashboard section + write-up — surface the Spain-vs-EU comparison (N14) in `docs/index.html` + short report, V36 caveats visible to the reader. | I.*,V36 |
| N16 | . | D,F | MENA/asylum fiscal-cost estimate — from N6 budget data, estimate taxpayer cost of state support for unaccompanied minors (MENAs) + asylum seekers, per-capita and aggregate, national+regional sources; document assumptions/coverage gaps. | C1,C15 |
| N17 | . | D,F | MENA/asylum crime attribution + base rates — from N6 crime data, quantify criminal-case counts + offence-type breakdown attributed to MENAs/asylum seekers as absolute counts + base rates (denominator+source stated), not standalone relative shares; address public-discourse claims directly with caveats. | V14,C1 |
| N18 | . | A,C,D | Asylum acceptance-rate analysis (Spain vs Italy/Germany) — from N19 data, compute Spain's asylum applications/year × country of origin, recognition (acceptance) rate, and median processing delay, and benchmark vs Italy, Germany & the EU average to test the "abnormally low acceptance" claim; examine drivers (origin mix, safe-country designations, subsidiary vs refugee status, backlog capacity) and trace downstream effects — rejected-but-not-returned irregular-stay stock (N4/N11), overall migration flows (§C), crime base rates (N17), women's safety (§A). Associative only. | V14,C8,V36,V25 |
| N19 | . | C,infra | Acquire asylum-application statistics — applications/year × country of origin, first-instance + final recognition rate, and median/average processing time, for Spain + comparators (Italy, Germany, EU27), from Eurostat `migr_asy*` / EUAA (ex-EASO) + CEAR reports + Spanish OAR; fetch+parse only, analysis in N18. Document comparability caveats (first-instance vs final, refugee vs subsidiary status). | C1,C15,V36 |

---

## §B — Bugs

| id | date | cause | fix |
|---|---|---|---|
| B1 | 2026-05-20 | Haiku 2024 femicide count = 58; verified = 47 (−19%) | V2,V5 |
| B2 | 2026-05-20 | Haiku 2024 total sexual crimes = 21159; verified = 22846 (−7.4%) | V2,V5 |
| B3 | 2026-05-20 | Haiku 2024 agresiones sin penetración = 15953; verified = ~13673 (−14%) | V2,V5 |
| B4 | 2026-05-20 | Haiku 2023 total sexual crimes = 19981; verified = 21825 (−8.4%) | V2,V5 |
| B5 | 2026-05-20 | Haiku "62× cifra oculta" = invalid: divides annual denuncias by lifetime prevalence (incompatible units) | V11 |
| B6 | 2026-05-20 | Three incompatible violaciones series exist, not two. Serie A = MIR Anuario (3716–5453, includes Art.179 agresión + Art.181 abuso with penetration). Serie B = MIR Informe/Portal (2136–1873 for 2017–2019, Art.179 only — this is what Geo VS uses). Serie C = unknown series (1118–1520, origin unconfirmed; may be pre-2012 partial-territory data or a further subcategory). PARTIAL RESOLUTION: A vs B explained by legal category aggregation. Origin of C still unresolved. See fuentes_secundarias_analisis_espana.md §20. | V3,V6 |
| B7 | 2026-05-20 | My arithmetic critique of INE Condenados 2024 was wrong: 90 violaciones is subcategory of 1389 not additive; Haiku figures confirmed correct | V2 |
| B8 | 2026-07-01 | Consolidated sexual-crimes-MIR output filename hardcoded as `sexual_crimes_mir_2000-2024.csv` in `mir_parser.py` `main()`, regardless of which years were actually parsed (real coverage 2019–2024 only — no MIR Informe PDFs exist pre-2019); user caught the mismatch directly | V19 |
| B9 | 2026-07-01 | `mir_parser.py` `CategorySexBreakdown` carried `source_page`/`confidence` per category row, though both are structurally constant across every category in a report (`source_page` set once per report via `_upsert()` in `_extract_typology()`; `confidence` never varies per category) — reproduced the exact repeated-data problem the CSV→JSON migration was meant to fix; user caught it via `/backprop` | V20 |
| B10 | 2026-07-01 | `mir_parser.py` `main()` wrote one JSON file per parsed year AND a separate "consolidated" file with the same reports, for `--pdf-dir` runs — redundant duplicate artifacts; user caught it via `/backprop` | V21 |
| B11 | 2026-07-01 | `run_batch()` (the V21 fix) had no year-collision check; `--pdf-dir data/sources` (now flattened per user's PS) silently merged 2 PDFs each for 2023 (`MIR_Informe_DelitosSexuales2023.pdf` + `MIR_GroupSexualViolence_2023.pdf`) and 2024 (`MIR_Informe_DelitosSexuales2024.pdf` + `VMujeres_2024.pdf`) into one MIRDataset; caught by `/review` during this same backprop cycle, not by the user | V22 |
| B12 | 2026-07-02 | §T section duplicated wholesale (stale block vs current block), causing T17 to collide across two unrelated tasks (mortality-by-age/sex/cause vs dashboard-tabbed-refactor); caught by `/check` | V26,V27 |
| B13 | 2026-07-02 | `compute_feminicide_rates.py:30` hardcoded input path `feminicidios_delegacion_2003-2026.csv` — aspirational range, file was renamed to `feminicidios_delegacion_2024.csv` (real coverage), script was broken (input missing) until this cleanup | V19 |
| B14 | 2026-07-02 | §I dashboard description was a flat "tab1/2/3/4" list that didn't match `docs/index.html`'s real nested structure (3 main tabs, `violence` itself holding 3 sub-tabs); no invariant previously required §I to track real DOM structure | §I corrected this cleanup — no prior invariant caught it |
| B15 | 2026-07-02 | T27 status was `.` (not started) while its target artifact `migrant_crime_numerator.csv` already held 43 populated `confidence=high` rows, generated by an undocumented parser (`mir_migrant_nationality_parser.py`) absent from §I | V33 |
| B16 | 2026-07-02 | First-pass Explore-agent scan flagged `feminicide_extractor.py`, `mir_violence_parser.py`, `mir_violence_extractor.py` as dead code from import-graph analysis alone; git history + PDF/output-coverage check showed all 3 are live, undocumented generators (T20, T32-input) — import-graph-only "unreferenced" was insufficient evidence; user caught it by asking for a deeper check | V34 |
| B17 | 2026-07-02 | `mir_violence_parser.py`'s `to_num()` stripped thousands-dots but not decimal-commas, silently truncating any Spanish decimal value (e.g. "12,5" → 12 not 12.5); fixed by adopting the shared `parse_es_number()` from `src/parsers/utils.py` | V35 |
| B18 | 2026-07-02 | `feminicide_parser.py` Table 2.2/2.3 regex extraction quality has regressed relative to the currently-committed CSV (0 age rows, garbled origin categories) — pre-existing, not caused by this cleanup's refactor; `batch_extract()` ! not re-run against live data without re-verifying extraction quality first | V12 |
| B19 | 2026-07-02 | `mir_migrant_nationality_parser.py`'s regex-based extraction is fragile to `pdftotext`/poppler version differences — a clean re-run on this machine produced 6 fewer correct rows + 2 spurious null-count rows than the committed CSV, with no code changes involved; ! not re-run against the live output path without diffing first | V12 |
| B20 | 2026-07-02 | `mir_violence_parser.py`/`mir_violence_extractor.py` extract 0 rows against the default page range (52-57) — pre-existing `pdftotext`/report-layout mismatch, confirmed via before/after diff, unrelated to this cleanup's refactor; documented in `data/PIPELINE.md`'s parser table | V12 |
| B21 | 2026-07-02 | B18's diagnosis (regex extraction quality regression) was itself built on a false premise from an earlier session: Windows Git-Bash's console codepage was garbling non-ASCII Spanish text on display, wrongly read as a broken font ToUnicode CMap in the source PDFs (literal U+FFFD "corruption"). Verified via scratchpad-file round-trip (write extracted text to a file, `Read` tool back) that both `pdftotext` and `pdfplumber` decode the PDFs correctly with zero U+FFFD chars. Consolidated `feminicide_parser.py` rewritten with literal accented regex patterns (not a `�`-placeholder workaround); re-validated against all 24 PDFs 2003-2026 — sum(regional) = total_victims exact for all 21 modern-format years, B18 no longer reproduces | V12 |
| B22 | 2026-07-03 | `data/sources/delegacion_gobierno_femicidio.md`'s manually curated table (secondary-source, pre-full-parser) diverged from the freshly parsed primary PDFs in most years by 1-6 victims (e.g. 2024: 47→49, 2018: 47→52, 2020: 44→50) and in 2024's origin split (51% foreign→49% foreign); every source PDF carries a recent `update_date` (Oct 2025-Jun 2026), showing Delegación revises historical years retroactively as "en investigación" cases resolve — the doc's old figures were a stale point-in-time snapshot, not wrong data entry. Doc rewritten to source 2006-2026 from the parsed JSON (high confidence) and flag this as an ongoing-revision caveat, not a one-time fix | V2,V12 |
| B23 | 2026-07-03 | B6's "irreconcilable ~4× violaciones series" claim does not hold for 2021-2023: new `AnuarioParser` (T21) direct-parsed Anuario 2021/2022/2023 (`data/raw/sexual_crimes_mir_anuario_2021-2023.json`) and cross-checked against the already-verified Informe series — total_count AND the Art.179-equivalent subcategory match exactly for all 3 years (17016/19013/21825 totals; 2143/4270/4890 violaciones). B6's original divergence is now scoped to pre-2019 years only (secondary-sourced, not yet re-parsed from primary PDFs). Separately found: 2023 "Pornografía de menores" disagrees between the two publications (Informe 836 vs Anuario 909) though both totals still reconcile to 21825 — confirmed via direct PDF text dump (Anuario 2023 p.104 literally prints 909) that this is a genuine cross-publication source anomaly, not a parser bug. Documented in `data/sources/mir_informes_delitos_sexuales.md` and `SOURCES_INDEX.md` | V13 |
| B24 | 2026-07-03 | MIR "Balance de Criminalidad" quarterly PDFs (T55) are CUMULATIVE year-to-date, not incremental per-quarter — each PDF's NACIONAL table is labeled "Acumulado enero a marzo/junio/septiembre/diciembre"; naively summing all 4 quarters overcounts the true annual figure by ~2.4-2.6× (e.g. 2023: naive sum 48895 vs true annual 19981). `run_balance_batch` fixed to build each year's `MIRReport` from the Q4 ("enero a diciembre") report alone; Q1-Q3 are still parsed and logged for evidence but discarded. Cross-validated against Anuario/Informe (B23): 2019-2021 near-exact/exact match (within 0.5%); 2022-2024 diverge 7-9% on the total (con-penetración subcategory stays close except 2022, off ~33%) — Balance pages self-flag "Datos pendientes de consolidar" (data pending consolidation), most plausibly explained as Balance being a provisional snapshot vs. Anuario/Informe's later-published, fully consolidated figures; not yet root-caused to a specific missing police body. Documented in `data/sources/mir_informes_delitos_sexuales.md` and `SOURCES_INDEX.md` | V12,V13 |
| B25 | 2026-07-03 | `run_balance_batch`'s first implementation appended an empty `MIRReport` (0 categories, no total) for any year whose Q4 PDF *file* existed but whose table extraction failed — the guard only checked `"Q4" in quarters` (file present), not whether `BalanceParser.parse()` on that file actually returned records. Manifested for 2016-2018 (pre-2019 table layout, see B26) once the batch was run against the full 2016-2026 PDF range rather than the originally-scoped 2021-2023; found by directly inspecting the output JSON (3 reports with 0 categories) rather than trusting the batch run's "10 report(s)" success message. Fixed by adding `if not q4_records: skip` before appending the report; re-run correctly produced 7 clean reports (2019-2025) with 2016-2018 logged as explicit skips, not silently emitted as empty/wrong data | V12,V19 |
| B26 | 2026-07-03 | 2016-2018 `MIR_BalanceCriminalidad` PDFs use an older NACIONAL-table layout that `BalanceParser`'s current regex/page-scan logic does not match (`row 5 (total) not found` for all 2016 quarters; `year not found in header columns []` for 2017-2018 and 2019 Q1) — same class of pre-2019 layout-instability problem as the already-documented 2017/2018 `InformeParser` failure. Not reverse-engineered this round; `run_balance_batch` (post-B25 fix) cleanly detects and skips these years rather than emitting wrong data. Open gap, tracked as a known limitation in `data/raw/sexual_crimes_mir_balance_2019-2025.json`'s coverage (2019-2025 only) and documented in `data/sources/mir_informes_delitos_sexuales.md` | V12 |
| B27 | 2026-07-03 | `discurso_odio_inmigracion_espana.md`'s "EU-MIDIS Spain-specific breakouts: none found, EU-aggregate only" claim was incomplete — EIGE/FRA research pass (`data/sources/eige_fra_comparative.md` §3) found a live Spain country-sheet PDF URL, FRA's Spain-preconfigured data-explorer view, and an academic paper (REIC, Bermejo et al. 2021) analysing Spain's own N=1,563 EU-MIDIS II subsample; the specific 88%/90%/72% violence/harassment-reporting-rate figures already cited are still not isolated to Spain (needs PDF text extraction, see T57) — claim softened from "not found" to "not yet extracted" | V18 |
| B28 | 2026-07-04 | MIR "Informe de Delitos de Odio" has no dedicated 2022 edition (publication gap, not a parser failure) — the 2023 edition's own backward-comparison column shows 2022 figures by subtraction (TOTAL DELITOS 1796, headline 1869), but `run_odio_batch` deliberately does NOT synthesize a full 2022 `MIRReport` from that secondary-sourced column (would conflate primary vs retrospective sourcing, same class as B23's retroactive-restatement risk); output filename `hate_crimes_mir_2016-2021_2023.json` honestly shows the non-contiguous range per V19. Separately: the `DISCAPACIDAD` ámbito (262 in 2016) is renamed `DIVERSIDAD FUNCIONAL` in 2017 and drops to 23 (−91.2%) — the 2017 report's own prose attributes this to a definitional/methodology change, not a real collapse; `classify_odio_category` maps all label variants to one key for series continuity but the pre/post-2017 break is non-comparable, same caveat class as the sexual-crimes LO 10/2022 break (B6/C3). Both caveats documented in `data/sources/mir_delitos_odio.md` | V12,V19 |
