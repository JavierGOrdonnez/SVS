# SPEC.md — SVS (Spain Violence Statistics)

Shared entrypoint for all agents working in this repo. This file holds the
project-wide goal, the constraints/invariants that apply everywhere, the
roadmap/phase narrative, the infra-only tasks, the N1-N19 expansion threads,
and the full chronological bug log. Domain-specific tasks, invariants, and
interfaces live in each module's own spec, co-located with its code:

- [`src/feminicides/SPEC-feminicides.md`](src/feminicides/SPEC-feminicides.md)
- [`src/sexual_crimes/SPEC-sexual-crimes.md`](src/sexual_crimes/SPEC-sexual-crimes.md)
- [`src/crime/SPEC-crime.md`](src/crime/SPEC-crime.md) (nationality/peligrosity, hate-speech-migration correlation, hate crimes)
- [`src/migration/SPEC-migration.md`](src/migration/SPEC-migration.md)
- [`src/mortality/SPEC-mortality.md`](src/mortality/SPEC-mortality.md)
- [`src/analysis/SPEC-analysis.md`](src/analysis/SPEC-analysis.md) (cross-cutting probability/regression/relationship/funnel/EU-comparative work + shared population denominator + dashboard orchestrator)

`src/parsers/` and `src/download_reference_documents.sh` are shared
infrastructure referenced by every module and stay undivided; see
`data/PIPELINE.md` for their script-level detail.

---

## §G — Goal

Compute annual, 5-year & lifetime cumulative probability of rape/sexual-assault/femicide/homicide/non-sexual-violence for woman born 2000 ∈ Spain, holding 2025 conditions fixed. Extend: estimate partial effects of covariates (far-right vote share, immigration volume & composition) using 2000–2025 historical series.

## Key Quantities & Insights
- **A. Victim risk profile** — for rape / sexual-assault / femicide / other-homicide / non-sexual-violence + combined "any violence": incidence/100k (2025) → 1yr/5yr/lifetime cumulative P, both **reported** and **dark-figure-corrected**. See [`SPEC-analysis.md`](src/analysis/SPEC-analysis.md) T8, T9.
- **B. Historical trends** 2000–2025 with definition-break annotations (largely in the dashboard already).
- **C. Covariate effects** (associative) — violence-rate ~ far-right vote-share + immigration volume/composition; ±10/20% scenario table. See [`SPEC-analysis.md`](src/analysis/SPEC-analysis.md) T10, T12, T13.
- **D. Peligrosity (umbrella)** — per-capita perpetrator rate, **bracket [convicted ↔ identified] per 100k men**, by age × nationality × origin (later socioeconomic). Always paired with absolute framing per V14. See [`SPEC-crime.md`](src/crime/SPEC-crime.md) T26-T31, T41, T42.
- **E. Victim–aggressor relationship structure** — relationship distribution + victim↔aggressor matrix (sexual assault, ±penetration); produces adjustment factors that feed back into A and D. See [`SPEC-analysis.md`](src/analysis/SPEC-analysis.md) T32, T33.
- **F. Hate-speech & Migration correlation** — analyze hate-speech trends in Spain + correlates (media presence, polling/worry, xenophobic political party voting, migration-associated crime data) via watchdog reports & primary sources. See [`SPEC-crime.md`](src/crime/SPEC-crime.md) T37–T40.
- **G. GBV non-sexual justice funnel** (*later expansion*) — denuncias → diligencias → condenas + reporting rate, for physical/psychological GBV. See [`SPEC-analysis.md`](src/analysis/SPEC-analysis.md) T34.

---

## §C — Constraints

C1: All claimed statistics ! cite exact source + table/figure + year of publication.  
C2: Reported-crime data ≠ actual incidence; dark-figure correction ! attempted via survey cross-validation.  
C3: Definition of each offence ! documented per year — Spanish penal code changed materially in 2022 (LO 10/2022 "Solo sí es sí") & 2023 reform; pre/post comparability ⊥ assumed without explicit bridge.  
C4: "Femicide" in official data = intimate-partner/ex-partner homicide only (Delegación del Gobierno registry) ≠ all female homicides ≠ academic broad definition.  
C5: Macroencuesta (victimisation survey) ! used to bound dark figure; conducted irregularly (2011, 2015, 2019); interpolation between waves ? unreliable.  
C6: Age-specific rates ! used wherever available; aggregate rates ∈ fallback only.  
C7: ∀ probability estimates → competing-risks life-table, not naive annual-rate multiplication (events not independent across years).  
C8: Covariate regression = associative, ⊥ causal.  
C9: Prior AI model (Haiku) likely hallucinated point estimates without source verification → ∀ numbers from that conversation ! re-verified against primary sources before use.  
C10: Population denominator ! female population of Spain from INE Padrón; age-stratified where possible.  
C11: Ethnicity/nationality breakdowns sparse in official data; include when available, flag absence.  
C12: ∀ CSV rows ! have `confidence` ∈ {high, medium, low, unverified} — unverified rows ⊥ used in final estimates until confirmed.  
C13: PDF extraction parsers ! assume stable table formats: Delegación del Gobierno 1-page format (2003–2026), MIR Informe standard tables (2019–2024), MIR Anuario TABLA 3-1-5/3-1-6 dense whitespace-aligned text layout — no dedicated per-category chapter, no ruling lines (2021–2023 PDFs sourced so far; 2000–2020 gap, no PDFs obtained yet); format deviations ⊥ silently skipped; parser ! halt and report inconsistency. Source PDFs for all MIR series (Anuario, Informe delitos libertad sexual, Balance de Criminalidad trimestral, Informes de Delitos de Odio) ! sourced from `https://estadisticasdecriminalidad.ses.mir.es/publico/portalestadistico/publicaciones.html` — the SES portal is the canonical hub for these; per-year direct URLs go in the relevant `data/sources/*.md` doc, not here.  
C14: Python parser stack requires pdfplumber (table extraction), pandas (aggregation + validation), pydantic (schema validation for nested JSON exports), openpyxl (fallback for Excel tables); validation failures ! block output (no partial extracts).  
C15: Literature-first: published studies/syntheses ! preferred where they exist, but ∀ secondary figure → primary source ! traced & documented; secondary value ⊥ cited as if primary. (extends C1; the 29-source catalogue in `fuentes_secundarias_analisis_espana.md` is the working set.)  
C16: Agent ⊥ ever set/mark any `confidence` field (CSV/JSON rows, `docs/index.html` component attrs, code constants like `CONF_DELEGACION`, or a §V/§C confidence rule itself) to `high` — even under the user's direct in-the-moment instruction to do so. Only the user may make that specific edit; agent's role in that situation ! point to the exact file/line where the user themself would make the change.

---

## §R — Research

R1: Eurostat vs INE migration/population parsers — investigated whether
`src/migration/parse_eurostat_migration_cohort.py`, `src/analysis/parse_ine_population.py`,
`src/mortality/parse_ine_mortality.py`, `src/crime/parse_ine_tabla28716.py` are near-duplicate scripts
that should be merged. Verdict: **not duplicative** — each targets a distinct
source format + delivery mechanism, and only one (`parse_eurostat_migration_cohort.py`)
touches migration at all; there is no separate INE-migration parser script in
`src/` to compare it against (`migration_spain.csv`'s base T11 data, 2337 rows,
was built in a merged branch, not a script that lives here today).

| | `parse_eurostat_migration_cohort.py` | `parse_ine_population.py` | `parse_ine_mortality.py` | `parse_ine_tabla28716.py` |
|---|---|---|---|---|
| source | Eurostat bulk SDMX TSV (`migr_imm1ctz`/`migr_pop1ctz`), manual gunzip download, not committed | INE table 56934 CSV, semicolon-delimited, `;` sep | INE table 7947, JSON API dump (`wstempus`) | INE table 28716 CSV, semicolon-delimited |
| fetch | manual (large bulk file, user downloads + gunzips) | manual CSV (could be scripted, isn't) | manual JSON dump | **live HTTP fetch** via `requests` inside the script |
| number format | plain int + trailing flag chars (`b`,`e`,`p`,`@N`) stripped by regex | Spanish thousands-dot, some post-2021 Censo rows plain int — both handled inline | plain int (JSON) | Spanish thousands-dot + decimal-comma, `pandas`-based |
| dimension shape | wide TSV, one column per year, dims packed into first column comma-joined | long CSV, one row per (age, sex, period) | nested JSON, `nombre` field encodes sex+age+cause in two inconsistent string formats (regex-split) | long CSV, one row per (crime_type, nationality, year) |
| output | appends to shared `migration_spain.csv` (idempotent: drops+regenerates its own joint-cross rows on rerun) | writes 2 new CSVs (`population_spain_estimates.csv` full long form, `population_spain_midyear_5yr.csv` binned to match mortality table's age bins) | writes 1 new CSV, columns include a `series_cod` for traceability | writes 2 new CSVs (raw counts + derived nationality %) |
| why separate | different bulk-download source entirely (Eurostat, not INE); different join granularity (age×sex×citizenship cross INE itself doesn't publish past 2021/2022) | population is a distinct target quantity (denominator) from migration flow/stock | mortality is a distinct target quantity again, and INE's JSON API differs structurally from its CSV exports | crime convictions by nationality is a distinct target quantity; also the only one of the four with a live network fetch built in |

Conclusion: the shared pattern across all four is "some INE/Eurostat tabular
dump → tidy CSV", but the actual parsing logic (delimiter, number format,
dimension encoding, fetch mechanism) differs enough per source that a merged
parser would need a source-specific branch for every step anyway — same
non-duplication verdict as T53 reached for `src/parsers/`'s shared `utils.py`
extraction (that consolidation targeted truly identical logic — pdftotext
subprocess calls, CSV header guards — not source-specific parsing).

---

## §I — Interfaces (shared)

```
file: data/raw/violence_spain.csv                      → append-only raw data log, written into by every domain (T1-T5)
out:  reports/                                         → final write-ups & charts
html: docs/index.html (3 main tabs: violence, mortality, migration; `violence` nests 3 sub-tabs: sexual-crimes+Macroencuesta, feminicides, others [domestic-violence denuncias, all-cause homicide, EIGE sex/relationship breakdown, victim-perp matrix, homicide rate trend, confidence-distribution chart]) → interactive dashboard with rich visualizations, fetches docs/data/*.json (written by src/analysis/build_dashboard.py, see SPEC-analysis.md)
doc:  `data/PIPELINE.md`                               → script-level companion to this §I: every script under src/ + src/parsers/, its reads/writes, and its §T task (V33 orphan check)
```

Domain-specific files/cmds (raw datasets, per-module analysis scripts,
dashboard data builders, source docs) live in each module's own §I — see the
links at the top of this file.

---

## §V — Invariants (shared)

V1: ∀ row ∈ violence_spain.csv → `row_id`, `violence_type`, `year`, `value`, `unit`, `source_name`, `source_table`, `confidence` ! non-empty.  
V2: `confidence=high` → source is primary govt/official publication, value directly readable (not computed by prior AI).  
V3: `confidence=medium` → source is primary but value requires minor computation (e.g. rate from count + population) or source is reputable secondary.  
V4: `confidence=low` → source is ambiguous, secondary, or value inferred.  
V5: `confidence=unverified` → value originates from prior AI conversation; ⊥ used in modelling until cross-checked.  
V6: ∀ rate computation → denominator ! stated (female population, female 15-49, etc.).  
V12: ∀ extracted dataset → parser ! validate: sum(category_breakdown) = reported_total, sum(age_groups) = reported_total, sum(region/origin/demographic breakdowns) reconcile to headline count; validation ⊥ pass until all cross-checks pass. Discrepancies ! logged with row ID + expected vs actual + PDF page reference.  
V19: Generated dataset filenames ! computed from the actual min/max year of the records they contain (e.g. `f"sexual_crimes_mir_{min_year}-{max_year}.csv"`), ⊥ hardcoded aspirational ranges; §I file-path documentation ! kept in sync whenever the real range changes.  
V20: ∀ nested-JSON schema → a field lives at the level where its value can legitimately differ between siblings; if a field is structurally constant across every child of a parent (e.g. `source_page`/`source_table`/`verified` — same for every category in one report), it ! live on the parent only, ⊥ duplicated per child.  
V21: ∀ multi-input parser run (batch/dir mode) → exactly one output dataset artifact ! written, named by the actual year range covered; ⊥ one-file-per-input plus a redundant consolidated copy of the same data.  
V22: ∀ MIRDataset built from a batch/dir run → report years ! unique; ∃ 2 input PDFs inferring the same year → run_batch ! raise (loud failure listing the colliding filenames), ⊥ silently include both in one dataset.  
V26: this file and each `SPEC-<module>.md` ! contain exactly one `## §T` section header and one §T pipe table each; amendments to a §T table ! edit the existing table in place, ⊥ append a new section/table. A task's row lives in exactly one file (this file for infra/N-thread tasks, its owning module's SPEC otherwise) — see §T's module-map below.  
V27: ∀ §T task id ! unique across the whole spec (this file + all `SPEC-<module>.md` combined); ⊥ two rows share one id, whether in the same file or across files.  
V28: §T row prose ! stay task-description length (what/where/status); once a task's results/analysis grow past that, they ! move to `reports/<name>.md` with the row keeping a 2-3 sentence summary + link.  
V29: ∀ §T row → `track` column ∈ {A,B,C,D,E,F,G,infra} (comma-separated if cross-cutting), matching §G's lettered Key-Quantities list.  
V33: ∀ script ∈ `src/` ∪ `src/parsers/` → row ! exist in `data/PIPELINE.md` naming its §T task; row with no §T task, or script with no row, = orphan by definition and ! resolved (task added or script archived) before considered done.  
V34: before any file/script is deleted or archived as "superseded" → target report/PDF/data coverage of the candidate ! checked against the file(s) claimed to supersede it, not the import graph alone; import-graph-only "unreferenced" is insufficient evidence of dead code.  
V35: PDF text extraction, CSV row writing, and Spanish-number parsing in `src/parsers/` ! go through `src/parsers/utils.py`, ⊥ reimplemented per-file.

Domain-specific invariants (V7-V11, V13-V18, V23-V25, V36) live in each
module's own §V — see the links at the top of this file.

---

## §T — Tasks (shared: roadmap, infra, expansion threads)

### Roadmap

- **Phase 0 — housekeeping/now**: sync T11/T-mig-tab status; rewrite README + mermaid (change #5); create `reports/results.md` (change #2); expand §G/§C/§V (changes #1, #3).
- **Phase 1 — finish core violence series** (unblocks life table): T2 ([mortality](src/mortality/SPEC-mortality.md)), T21, T22 ([sexual_crimes](src/sexual_crimes/SPEC-sexual-crimes.md)), T4 ([crime](src/crime/SPEC-crime.md)), T5, T14, T35 ([analysis](src/analysis/SPEC-analysis.md), literature synthesis → dark-figure & relationship priors).
- **Phase 2 — core probability deliverable** *(current priority)*: T8, T9 ([analysis](src/analysis/SPEC-analysis.md)), T24 ([feminicides](src/feminicides/SPEC-feminicides.md)), T15 (now incl. extraction map + composition DAG), T16.
- **Phase 3 — peligrosity & relationship**: T30, T31 (peligrosity engine + bracket), nationality slice T26-T29 ([crime](src/crime/SPEC-crime.md)); T32, T33 (relationship structure + adjustment, [analysis](src/analysis/SPEC-analysis.md)).
- **Phase 4 — covariate regression & projections**: T10, T12, T13 ([analysis](src/analysis/SPEC-analysis.md)).
- **Phase 5 — later expansions**: T34 (GBV non-sexual funnel), T36 (docs composition/methodology diagram), peligrosity for other petty crimes, T54 (analysis-script consolidation backlog), T56/T57 (EU-comparative GBV data — Eurostat gbv_* API + EU-MIDIS II Spain extraction), T58 (EIGE administrative-indicator pull, backlog) — all [analysis](src/analysis/SPEC-analysis.md).
- **Expansion threads (N1–N19)** *(added 2026-07, mirrored 1:1 in Project House milestones M1–M8)*: data-intake widening (N1–N6: INE, CIS concern series, migrant outflows, undocumented-stock estimates, political/TV discourse corpus, MENA/asylum sources); transversal dark-figure/reporting-rate layer (N7 policy annotation, N8 reporting-rate drift, N9 correction layer); migration-denominator rigour (N10 net-stock, N11 undocumented sensitivity); discourse & perception (N12 salience, N13 perception-vs-phenomenology); EU comparison (N14 analysis, N15 dashboard); MENA/asylum (N16 fiscal cost, N17 crime attribution); asylum acceptance-rate deep-dive (N19 data, N18 Spain-vs-IT/DE analysis + downstream effects).

### Active work plan (2026-07-20 session): consolidate feminicides → sexual violence → hate crimes

Sequenced fix-up pass across the three flagship datasets, run against the
now-modular `src/` + split-SPEC structure this section's Phase 0 established.
Each phase's concrete bullet-point fixes live in that phase's own module
spec, not here:

- **Phase A — feminicides**: fix `compute_feminicide_rates.py`'s circular population estimate, extend to 2006-2024, add parser tests. See [`SPEC-feminicides.md`](src/feminicides/SPEC-feminicides.md).
- **Phase B — sexual violence**: populate remaining `violence_spain.csv` gaps, annotate the Balance-vs-Anuario discrepancy (B24), add Anuario/Balance parser tests. See [`SPEC-sexual-crimes.md`](src/sexual_crimes/SPEC-sexual-crimes.md).
- **Phase C — hate crimes**: close the `data/PIPELINE.md` odio-mode gap, install pytest as a real dev dependency, flag the missing 2024 hate-crimes PDF. See [`SPEC-crime.md`](src/crime/SPEC-crime.md).

### Module map (which file owns which §T id)

| module spec | owns |
|---|---|
| [SPEC-feminicides.md](src/feminicides/SPEC-feminicides.md) | T1, T19, T20, T23, T24 |
| [SPEC-sexual-crimes.md](src/sexual_crimes/SPEC-sexual-crimes.md) | T3, T21, T22, T42, T55 |
| [SPEC-crime.md](src/crime/SPEC-crime.md) | T4, T26-T31, T37-T41, T45-T48, T50, T51, T59, T76-T83 |
| [SPEC-migration.md](src/migration/SPEC-migration.md) | T11, T-mig-tab, T43, T44 |
| [SPEC-mortality.md](src/mortality/SPEC-mortality.md) | T2, T7, T49 |
| [SPEC-analysis.md](src/analysis/SPEC-analysis.md) | T5, T6, T8, T9, T10, T12-T16, T32-T36, T52, T54, T56-T58 |
| this file (below) | T17, T18, T25, T53, N1-N19 |

### §T pipe table (infra + expansion threads only)

| id | status | track | task | cites |
|---|---|---|---|---|
| T17 | x | infra | Restructure dashboard with tabbed interface — refactor `docs/index.html`: tab 1 = sexual crimes + Macroencuesta surveys, tab 2 = feminicides; maintain embedded source hyperlinks; add data-tab attribute to charts | I.*,V8 |
| T18 | x | infra | Download reference documents — fetch 4 additional synthesis reports from FURTHER_SOURCES.md (MIR 2022/2023/2024 methodological reports, CCOO analysis, group-violence report, MTAS 2015–2019 overview); organize by type in `data/sources/` | C1 |
| T25 | x | infra | Restructure markdown docs — archive stale Haiku phase-log artifacts (`FASE_2_PLAN_2015_2020`, `FASE_3_PLAN_2000_2014`, `FASE_2_PROGRESO_TAREAS_2_1_2_3`, `FASE_2_TAREAS_2.7_2.8_CIERRE_FINAL`, `INFORME_PROGRESO_FASE1_COMPILACION`, `INFORME_PROGRESO_FASE2_TAREA2.1`, `INDICE_MAESTRO`) + `FURTHER_SOURCES.md` to `archive/`; merge `SEXUAL_VIOLENCE_VERIFICATION.md`+`QUICK_VERIFY_SEXUAL_VIOLENCE.md`+`MORTALITY_MIGRATION_VERIFICATION_GUIDE.md` → one `VERIFICATION_GUIDE.md`; add `data/sources/SOURCES_INDEX.md` (1-page index over the 10 living source docs: scope, confidence, known issues) | — |
| T53 | x | infra | Consolidate `src/parsers/` shared logic — DONE: created `src/parsers/utils.py` (`extract_text`, `write_csv_rows`, `parse_es_number`, `cli_require_arg`); migrated all 6 parser files to import from it, removing inlined pdftotext/CSV-writer/argv-guard duplicates; `mir_violence_parser.py`'s `to_num()` now delegates to `parse_es_number()` (real bug fix — old version silently dropped the fractional part of any decimal-comma value); brought `feminicide_extractor.py`/`mir_violence_extractor.py`/`mir_violence_parser.py` docstrings up to the standard of the other 3 files. Verified per-file via `git diff HEAD` (mechanical-only) + live re-run against source PDFs/data. Also produced `data/PIPELINE.md` (this task's script-map deliverable). | V35 |
| N1 | . | infra | Broaden INE table coverage — pull additional INE tables (Condenados t.28716 further years/offence detail, MIR/INE homicide sex-breakdown, other denominator/offence tables) into `data/raw`; fetch+parse only, downstream analysis in the consuming task. | C1,V33 |
| N2 | . | C,F | Acquire CIS "principales problemas del país" barometer concern-ranking series (top national concerns, 1st–4th place, 2000–2025); isolate immigration/crime/GBV-relevant categories; input to N13's perception analysis. | V18,C8 |
| N3 | . | C,infra | Acquire migrant outflow/emigration data (by nationality, age, sex) so net stock (inflow−outflow) can be computed for proper rate denominators (N10); document 2008/2021 method breaks; joinable to `migration_spain.csv`. | V10,C8,V25 |
| N4 | . | C,D | Source/estimate irregular (undocumented) migrant population stock (visa overstayers, unauthorized entries) + uncertainty; input to the undocumented-migration sensitivity bound (N11); trace the "minor vs legal migration" claim to primary sources. | V25,C10 |
| N5 | . | F | Acquire longitudinal political + mainstream-TV discourse dataset on immigration/crime/GBV topic salience, channel-separable (politicians vs broadcasters); structure only, analysis in N12. | V18 |
| N6 | . | D,F | Acquire MENA/asylum source data — (a) national+regional budget lines for unaccompanied-minor & asylum-seeker support; (b) criminal-case counts + offence types attributed to these populations, with population denominators; feeds N16/N17. | C1,V14 |
| N7 | . | A,B | Policy-change annotation layer — catalogue major Spanish GBV/sexual-offence legal changes 2000–2025 (LO 1/2004, LO 10/2022, 2023 reform) and annotate the reported series for before/after policy-effect reading, without conflating definition breaks (C3/V7) with real change. | C3,V7 |
| N8 | . | A | Reporting-rate time series & drift model — estimate how reporting propensity (not incidence) changed 2000–2025 (post-#MeToo disclosure shifts) per violence type; separate reporting-driven from incidence-driven trend components; transversal input to N9/§A/§C. | V11,C5 |
| N9 | . | A | Transversal correction layer — apply dark-figure multipliers (T9) + reporting-rate drift (N8) to each reported violence type → reported↔corrected incidence brackets feeding the life table (T8) and covariate analysis; every step documents its multiplier+source. | V11,C2 |
| N10 | . | C,D | Net-stock denominators — using outflow data (N3), recompute migrant stock as net (inflow−outflow) rather than inflow-only as the denominator for migrant peligrosity/victimization rates; quantify the difference vs inflow-only. | V6,V25,C10 |
| N11 | . | C,D | Undocumented-migration sensitivity — using N4 estimates, bound how irregular migration biases the numerator and denominator of migrant crime/victimization rates (both directions) and whether it materially moves headline estimates. | V25,C10,V14 |
| N12 | . | F | Longitudinal discourse-salience analysis — from N5's corpus, quantify net/percentage salience of immigration/crime/GBV topics over time, politicians vs mainstream-TV channels separately. | V18 |
| N13 | . | C,F | Perception vs phenomenology correlation — correlate real phenomenology (hate-crime/crime data) × discourse salience (N12) × public perception (CIS top-concern ranking, N2) over time; separate perceived from actual salience, examine lead/lag; associative only. | V18,C8 |
| N14 | . | A,F | Spain-vs-EU comparison analysis — from the EU-GBV pulls (T56/T57/T58) build the comparable Spain-vs-EU prevalence table (ES vs FR/DE/IT/SE/EU27) with V36 caveats (non-comparable EIGE admin indicators, Nordic-paradox disclosure gap, 2014-FRA-vs-2024-EU-GBV wave break). | V36,C15,V14 |
| N15 | . | infra | EU-comparative dashboard section + write-up — surface the Spain-vs-EU comparison (N14) in `docs/index.html` + short report, V36 caveats visible to the reader. | I.*,V36 |
| N16 | . | D,F | MENA/asylum fiscal-cost estimate — from N6 budget data, estimate taxpayer cost of state support for unaccompanied minors (MENAs) + asylum seekers, per-capita and aggregate, national+regional sources; document assumptions/coverage gaps. | C1,C15 |
| N17 | . | D,F | MENA/asylum crime attribution + base rates — from N6 crime data, quantify criminal-case counts + offence-type breakdown attributed to MENAs/asylum seekers as absolute counts + base rates (denominator+source stated), not standalone relative shares; address public-discourse claims directly with caveats. | V14,C1 |
| N18 | . | A,C,D | Asylum acceptance-rate analysis (Spain vs Italy/Germany) — from N19 data, compute Spain's asylum applications/year × country of origin, recognition (acceptance) rate, and median processing delay, and benchmark vs Italy, Germany & the EU average to test the "abnormally low acceptance" claim; examine drivers (origin mix, safe-country designations, subsidiary vs refugee status, backlog capacity) and trace downstream effects — rejected-but-not-returned irregular-stay stock (N4/N11), overall migration flows (§C), crime base rates (N17), women's safety (§A). Associative only. | V14,C8,V36,V25 |
| N19 | . | C,infra | Acquire asylum-application statistics — applications/year × country of origin, first-instance + final recognition rate, and median/average processing time, for Spain + comparators (Italy, Germany, EU27), from Eurostat `migr_asy*` / EUAA (ex-EASO) + CEAR reports + Spanish OAR; fetch+parse only, analysis in N18. Document comparability caveats (first-instance vs final, refugee vs subsidiary status). | C1,C15,V36 |
| N20 | ~ | C,D | Acquire 2025-2026 regularization-process data by nationality (Ministerio de Inclusión/SS/Migraciones or credible news) to test whether Algerians disproportionately "surfaced" relative to Moroccans, as an empirical proxy for H1's denominator-undercount hypothesis — SEARCHED, PARTIAL: official regularización extraordinaria 2026 breakdown found (1,174,978 applications; Colombia 25.9%, Morocco 13.3%, Venezuela 11.8%, Peru 8.8%, Honduras 4.9%; Africa 22.9% of total) but Algeria is not individually named — its share sits inside the "Africa minus Morocco" 9.6% residual shared with 7 other countries, not isolable from published figures. Morocco's 13.3% application share closely tracks its 14.8% existing registered-population share (no evident undercount); Algeria's bound (≤9.6%, vs its 1.4% existing share) is suggestive but not confirmable. See `reports/algeria_morocco_divergence.md`. Feeds N4/N11. | V25,C10,C1 |

---

## §B — Bugs

| id | date | cause | fix |
|---|---|---|---|
| B1 | 2026-05-20 | Haiku 2024 femicide count = 58; verified = 47 (−19%) | V2,V5 |
| B2 | 2026-05-20 | Haiku 2024 total sexual crimes = 21159; verified = 22846 (−7.4%) | V2,V5 |
| B3 | 2026-05-20 | Haiku 2024 agresiones sin penetración = 15953; verified = ~13673 (−14%) | V2,V5 |
| B4 | 2026-05-20 | Haiku 2023 total sexual crimes = 19981; verified = 21825 (−8.4%) | V2,V5 |
| B5 | 2026-05-20 | Haiku "62× cifra oculta" = invalid: divides annual denuncias by lifetime prevalence (incompatible units) | V11 |
| B6 | 2026-05-20 | Three incompatible violaciones series exist, not two. Serie A = MIR Anuario (3716–5453, includes Art.179 agresión + Art.181 abuso with penetration). Serie B = MIR Informe/Portal (2136–1873 for 2017–2019, Art.179 only — this is what Geo VS uses). Serie C = unknown series (1118–1520, origin unconfirmed; may be pre-2012 partial-territory data or a further subcategory). PARTIAL RESOLUTION: A vs B explained by legal category aggregation. Origin of C still unresolved. See fuentes_secundarias_analisis_espana.md §20. | V3,V6 |
| B7 | 2026-05-20 | My arithmetic critique of INE Condenados 2024 was wrong: 90 violaciones is subcategory of 1389 not additive; Haiku figures confirmed correct | V2 |
| B8 | 2026-07-01 | Consolidated sexual-crimes-MIR output filename hardcoded as `sexual_crimes_mir_2000-2024.csv` in `mir_parser.py` `main()`, regardless of which years were actually parsed (real coverage 2019–2024 only — no MIR Informe PDFs exist pre-2019); user caught the mismatch directly | V19 |
| B9 | 2026-07-01 | `mir_parser.py` `CategorySexBreakdown` carried `source_page`/`confidence` per category row, though both are structurally constant across every category in a report (`source_page` set once per report via `_upsert()` in `_extract_typology()`; `confidence` never varies per category) — reproduced the exact repeated-data problem the CSV→JSON migration was meant to fix; user caught it via `/backprop` | V20 |
| B10 | 2026-07-01 | `mir_parser.py` `main()` wrote one JSON file per parsed year AND a separate "consolidated" file with the same reports, for `--pdf-dir` runs — redundant duplicate artifacts; user caught it via `/backprop` | V21 |
| B11 | 2026-07-01 | `run_batch()` (the V21 fix) had no year-collision check; `--pdf-dir data/sources` (now flattened per user's PS) silently merged 2 PDFs each for 2023 (`MIR_Informe_DelitosSexuales2023.pdf` + `MIR_GroupSexualViolence_2023.pdf`) and 2024 (`MIR_Informe_DelitosSexuales2024.pdf` + `VMujeres_2024.pdf`) into one MIRDataset; caught by `/review` during this same backprop cycle, not by the user | V22 |
| B12 | 2026-07-02 | §T section duplicated wholesale (stale block vs current block), causing T17 to collide across two unrelated tasks (mortality-by-age/sex/cause vs dashboard-tabbed-refactor); caught by `/check` | V26,V27 |
| B13 | 2026-07-02 | `compute_feminicide_rates.py:30` hardcoded input path `feminicidios_delegacion_2003-2026.csv` — aspirational range, file was renamed to `feminicidios_delegacion_2024.csv` (real coverage), script was broken (input missing) until this cleanup | V19 |
| B14 | 2026-07-02 | §I dashboard description was a flat "tab1/2/3/4" list that didn't match `docs/index.html`'s real nested structure (3 main tabs, `violence` itself holding 3 sub-tabs); no invariant previously required §I to track real DOM structure | §I corrected this cleanup — no prior invariant caught it |
| B15 | 2026-07-02 | T27 status was `.` (not started) while its target artifact `migrant_crime_numerator.csv` already held 43 populated `confidence=high` rows, generated by an undocumented parser (`mir_migrant_nationality_parser.py`) absent from §I | V33 |
| B16 | 2026-07-02 | First-pass Explore-agent scan flagged `feminicide_extractor.py`, `mir_violence_parser.py`, `mir_violence_extractor.py` as dead code from import-graph analysis alone; git history + PDF/output-coverage check showed all 3 are live, undocumented generators (T20, T32-input) — import-graph-only "unreferenced" was insufficient evidence; user caught it by asking for a deeper check | V34 |
| B17 | 2026-07-02 | `mir_violence_parser.py`'s `to_num()` stripped thousands-dots but not decimal-commas, silently truncating any Spanish decimal value (e.g. "12,5" → 12 not 12.5); fixed by adopting the shared `parse_es_number()` from `src/parsers/utils.py` | V35 |
| B18 | 2026-07-02 | `feminicide_parser.py` Table 2.2/2.3 regex extraction quality has regressed relative to the currently-committed CSV (0 age rows, garbled origin categories) — pre-existing, not caused by this cleanup's refactor; `batch_extract()` ! not re-run against live data without re-verifying extraction quality first | V12 |
| B19 | 2026-07-02 | `mir_migrant_nationality_parser.py`'s regex-based extraction is fragile to `pdftotext`/poppler version differences — a clean re-run on this machine produced 6 fewer correct rows + 2 spurious null-count rows than the committed CSV, with no code changes involved; ! not re-run against the live output path without diffing first | V12 |
| B20 | 2026-07-02 | `mir_violence_parser.py`/`mir_violence_extractor.py` extract 0 rows against the default page range (52-57) — pre-existing `pdftotext`/report-layout mismatch, confirmed via before/after diff, unrelated to this cleanup's refactor; documented in `data/PIPELINE.md`'s parser table | V12 |
| B21 | 2026-07-02 | B18's diagnosis (regex extraction quality regression) was itself built on a false premise from an earlier session: Windows Git-Bash's console codepage was garbling non-ASCII Spanish text on display, wrongly read as a broken font ToUnicode CMap in the source PDFs (literal U+FFFD "corruption"). Verified via scratchpad-file round-trip (write extracted text to a file, `Read` tool back) that both `pdftotext` and `pdfplumber` decode the PDFs correctly with zero U+FFFD chars. Consolidated `feminicide_parser.py` rewritten with literal accented regex patterns (not a `�`-placeholder workaround); re-validated against all 24 PDFs 2003-2026 — sum(regional) = total_victims exact for all 21 modern-format years, B18 no longer reproduces | V12 |
| B22 | 2026-07-03 | `data/sources/delegacion_gobierno_femicidio.md`'s manually curated table (secondary-source, pre-full-parser) diverged from the freshly parsed primary PDFs in most years by 1-6 victims (e.g. 2024: 47→49, 2018: 47→52, 2020: 44→50) and in 2024's origin split (51% foreign→49% foreign); every source PDF carries a recent `update_date` (Oct 2025-Jun 2026), showing Delegación revises historical years retroactively as "en investigación" cases resolve — the doc's old figures were a stale point-in-time snapshot, not wrong data entry. Doc rewritten to source 2006-2026 from the parsed JSON (high confidence) and flag this as an ongoing-revision caveat, not a one-time fix | V2,V12 |
| B23 | 2026-07-03 | B6's "irreconcilable ~4× violaciones series" claim does not hold for 2021-2023: new `AnuarioParser` (T21) direct-parsed Anuario 2021/2022/2023 (`data/raw/sexual_crimes_mir_anuario_2021-2023.json`) and cross-checked against the already-verified Informe series — total_count AND the Art.179-equivalent subcategory match exactly for all 3 years (17016/19013/21825 totals; 2143/4270/4890 violaciones). B6's original divergence is now scoped to pre-2019 years only (secondary-sourced, not yet re-parsed from primary PDFs). Separately found: 2023 "Pornografía de menores" disagrees between the two publications (Informe 836 vs Anuario 909) though both totals still reconcile to 21825 — confirmed via direct PDF text dump (Anuario 2023 p.104 literally prints 909) that this is a genuine cross-publication source anomaly, not a parser bug. Documented in `data/sources/mir_informes_delitos_sexuales.md` and `SOURCES_INDEX.md` | V13 |
| B24 | 2026-07-03 | MIR "Balance de Criminalidad" quarterly PDFs (T55) are CUMULATIVE year-to-date, not incremental per-quarter — each PDF's NACIONAL table is labeled "Acumulado enero a marzo/junio/septiembre/diciembre"; naively summing all 4 quarters overcounts the true annual figure by ~2.4-2.6× (e.g. 2023: naive sum 48895 vs true annual 19981). `run_balance_batch` fixed to build each year's `MIRReport` from the Q4 ("enero a diciembre") report alone; Q1-Q3 are still parsed and logged for evidence but discarded. Cross-validated against Anuario/Informe (B23): 2019-2021 near-exact/exact match (within 0.5%); 2022-2024 diverge 7-9% on the total (con-penetración subcategory stays close except 2022, off ~33%) — Balance pages self-flag "Datos pendientes de consolidar" (data pending consolidation), most plausibly explained as Balance being a provisional snapshot vs. Anuario/Informe's later-published, fully consolidated figures; not yet root-caused to a specific missing police body. Documented in `data/sources/mir_informes_delitos_sexuales.md` and `SOURCES_INDEX.md` | V12,V13 |
| B25 | 2026-07-03 | `run_balance_batch`'s first implementation appended an empty `MIRReport` (0 categories, no total) for any year whose Q4 PDF *file* existed but whose table extraction failed — the guard only checked `"Q4" in quarters` (file present), not whether `BalanceParser.parse()` on that file actually returned records. Manifested for 2016-2018 (pre-2019 table layout, see B26) once the batch was run against the full 2016-2026 PDF range rather than the originally-scoped 2021-2023; found by directly inspecting the output JSON (3 reports with 0 categories) rather than trusting the batch run's "10 report(s)" success message. Fixed by adding `if not q4_records: skip` before appending the report; re-run correctly produced 7 clean reports (2019-2025) with 2016-2018 logged as explicit skips, not silently emitted as empty/wrong data | V12,V19 |
| B26 | 2026-07-03 | 2016-2018 `MIR_BalanceCriminalidad` PDFs use an older NACIONAL-table layout that `BalanceParser`'s current regex/page-scan logic does not match (`row 5 (total) not found` for all 2016 quarters; `year not found in header columns []` for 2017-2018 and 2019 Q1) — same class of pre-2019 layout-instability problem as the already-documented 2017/2018 `InformeParser` failure. Not reverse-engineered this round; `run_balance_batch` (post-B25 fix) cleanly detects and skips these years rather than emitting wrong data. Open gap, tracked as a known limitation in `data/raw/sexual_crimes_mir_balance_2019-2025.json`'s coverage (2019-2025 only) and documented in `data/sources/mir_informes_delitos_sexuales.md` | V12 |
| B27 | 2026-07-03 | `discurso_odio_inmigracion_espana.md`'s "EU-MIDIS Spain-specific breakouts: none found, EU-aggregate only" claim was incomplete — EIGE/FRA research pass (`data/sources/eige_fra_comparative.md` §3) found a live Spain country-sheet PDF URL, FRA's Spain-preconfigured data-explorer view, and an academic paper (REIC, Bermejo et al. 2021) analysing Spain's own N=1,563 EU-MIDIS II subsample; the specific 88%/90%/72% violence/harassment-reporting-rate figures already cited are still not isolated to Spain (needs PDF text extraction, see T57) — claim softened from "not found" to "not yet extracted" | V18 |
| B28 | 2026-07-04 | MIR "Informe de Delitos de Odio" has no dedicated 2022 edition (publication gap, not a parser failure) — the 2023 edition's own backward-comparison column shows 2022 figures by subtraction (TOTAL DELITOS 1796, headline 1869), but `run_odio_batch` deliberately does NOT synthesize a full 2022 `MIRReport` from that secondary-sourced column (would conflate primary vs retrospective sourcing, same class as B23's retroactive-restatement risk); output filename `hate_crimes_mir_2016-2021_2023.json` honestly shows the non-contiguous range per V19. Separately: the `DISCAPACIDAD` ámbito (262 in 2016) is renamed `DIVERSIDAD FUNCIONAL` in 2017 and drops to 23 (−91.2%) — the 2017 report's own prose attributes this to a definitional/methodology change, not a real collapse; `classify_odio_category` maps all label variants to one key for series continuity but the pre/post-2017 break is non-comparable, same caveat class as the sexual-crimes LO 10/2022 break (B6/C3). Both caveats documented in `data/sources/mir_delitos_odio.md` | V12,V19 |
| B29 | 2026-07-20 | `data/PIPELINE.md`'s Analysis table was missing rows for `src/feminicides/build_dashboard_data.py`, `src/sexual_crimes/build_dashboard_data.py`, `src/crime/build_dashboard_data.py` — all 3 exist and are called by `src/analysis/build_dashboard.py`'s `_load_domain()`, but the reorg's PIPELINE.md update (T53-adjacent cleanup) only added rows for the pre-existing mortality/migration builders + the new orchestrator, missing these 3 net-new per-domain files; a real V33 orphan gap, caught while cross-checking file contents against the table ahead of the SPEC.md module split | V33 |
| B30 | 2026-07-20 | `compute_feminicide_rates.py`'s `estimate_nationality_population()` derived female population by origin from a fixed assumed victim count (24/24, not even matching the real 2024 count of 25/24) divided by an assumed rate (1.68/8.32 femicides per million) sourced from the same Delegación ficha the script was meant to independently rate — the reported `rate_per_100k` just reproduced that same assumed input rate regardless of any real population data; the already-written real INE population loader (`load_population_data()`) was defined but never called from `compute_rates()`. Rewritten to derive population from real data only: total female population (`population_spain_midyear_5yr.csv`, summed across age groups) minus an estimated foreign-resident female population (`migration_spain.csv`'s `stock_foreign_nationality` series, scaled by the constant female-share ratio from 2025 — the only year INE publishes that series' sex split). `compute_rates()` extended from 2024-only to every year 2006-2024 (output renamed `feminicide_rates_2006-2024.csv` per V19, `build_dashboard_data.py` updated to select the latest year rather than assume a single-year file); `poisson_ci_95`'s docstring corrected to describe the actual normal-approximation CI it computes, not an exact Poisson CI | V6,C10,V19 |
| B31 | 2026-07-20 | `docs/app.js`'s `vline(x, label, color)` break-year annotation markers (fem-timeline COVID-2020, sx-totals LO-10/2022, hc-totals 2022-gap, mi-inflow 2008 EVR→EMCR) rendered at the chart's far right edge instead of at their labeled year. Root cause: `vline()` call sites pass `String(year)` but each chart's `data.labels` array held the raw numeric years straight from the JSON (`t.years`/`d.totals.years`/`d.annual_inflow.years`, all real ints per the Python builders). Traced against chartjs-plugin-annotation@3.0.1's own `scaleValue()` (numeric xMin bypasses `scale.parse` entirely, treated as a raw index; only a string xMin invokes `scale.parse`) and Chart.js@4.4.4's `CategoryScale.parse()` (does `labels.indexOf(raw)` with strict `===`; on no match, appends `raw` as a brand-new trailing label via `findOrAddLabel` — confirmed directly against the unminified source) — a string `"2020"` never strict-matches a numeric `2020` in the labels array, so the mismatched marker got appended as a phantom category past the real yearly bars, landing at the far right. Fixed by stringifying each affected chart's `labels` array (`.map(String)`) to match the already-stringified `vline()` values, not by changing `vline()` itself. No JS test harness exists in this repo (Python/pytest only) — verified via chart.js's own `CategoryScale.parse` source + confirmed JSON year fields are ints, not by an automated browser test; visual confirmation still pending user's own look at the live dashboard | V38 |
| B32 | 2026-07-20 | T63's original all-or-nothing V39 gate (`_parse_legacy_format()` in `feminicide_parser.py`) discarded an entire year's origin/age/regional breakdown if any one nationality/age/regional sub-total failed to reconcile exactly to the header total. Validating against the real 2004/2005 source PDFs surfaced that these are genuine, isolated single-block data-entry/rounding inconsistencies in the original Delegación ficha itself (2004: perpetrator-nationality row 52+21+0=73 vs header total 72; 2005: perpetrator-age row sums to 56 vs 57) — every other block for both years, including all victim-side data, reconciles exactly. Not a parser bug: confirmed against a raw PDF-text dump showing the mismatched rows' own printed percentages also don't sum to 100% in the source. Fixed by gating per-block instead of per-report: each of victim-nationality/perp-nationality/victim-age/perp-age/regional is checked independently; only the specific side (`victim_count`/`perp_count`) of the failing block is left `None`, all passing blocks (including the other role's counts in the same category) are still populated. Report `confidence` drops to `"low"` (from `"medium"`) whenever any block fails, with `notes` listing exactly which block(s) and by how much | V39 |
| B33 | 2026-07-20 | `_extract_victim_perp_table()` in `feminicide_parser.py` (shared by both the modern age-table and origin-table extraction) assumed a single fixed layout: split the block at `"Presuntos agresores"`, then read `2n` `Número`-then-numeric tokens before that split (victim count+pct) and `2n` after (perp count+pct). True for the origin table in every year and for the age table in 2023-2025, but the age table in 2006-2022 instead prints both actor labels ("Mujeres víctimas mortales"/"Presuntos agresores") as bare column headers BEFORE any `Número` token, with the real data ordered `[victim counts][perp counts][victim pcts][perp pcts]` flat across a single `4n`-token run rather than two separate per-actor `Número`/`%` sub-blocks. Splitting on `"Presuntos agresores"`'s position (a column header, not a section boundary, in this layout) landed before any numbers, so `victim_block` held zero `Número` matches and the function silently returned `[]` — 17 years (2006-2022) of age-breakdown data went missing from `data/raw/feminicidios_delegacion_2003-2026.json`, surfacing on the dashboard as a flat/non-stacked bar for those years. Fixed by detecting which layout a block uses (does `"Presuntos agresores"` appear before the first `"Número"`?) before deciding how to slice the token run | V40 |
| B34 | 2026-07-20 | `compute_feminicide_rates.py` applied a constant 2025 female-share ratio (49.52%) from `stock_foreign_nationality`'s only sex-disaggregated year to every year's foreign stock, yielding a frozen Spanish/foreign population split. This is wrong: foreign population composition shifted dramatically over time (Morocco: 35% female 2002 → 41.5% 2025; Colombia: 54.3%; Pakistan: 31.6%; Honduras: 64.2%). Rewritten to sum Eurostat `stock_nationality` per-year per-nationality sex-specific rows directly, replacing the constant-ratio derivation with the real joint age×sex×citizenship cross now in `migration_spain.csv` (T66 expansion) | V6,C10 |
| B35 | 2026-07-20 | `compute_feminicide_rates.py` computed Wald 95% CIs on raw feminicide counts (count ± 1.96·√count) and propagated them to rates via `poisson_ci_95()`. This CI captures only Poisson variance on small counts (N=24-50), not population-denominator uncertainty, and is trivially narrow — not useful for the dashboard's audience. Removed from code (`poisson_ci_95()` deleted, `ci_lower`/`ci_upper` dropped from CSV output), `build_dashboard_data.py` (CI fields no longer read), `docs/app.js` (tooltip simplified), and `docs/index.html` (CI caveat removed) | V6 |
| B36 | 2026-07-22 | Romania (RO) was in `parse_eurostat_migration_cohort.py`'s `CITIZENS` list (added by T44) and fully present with real data in the pre-downloaded `data/raw/eurostat_migr_pop1ctz_es.jsonl`, yet was absent from `migration_spain.csv`'s `stock_nationality` series (49 nationalities present, not the expected 50) — the CSV had gone stale relative to the already-updated JSONL sometime after T44/T66, unrelated to any code bug. Surfaced while re-running the parser for T69's Y_LT15/Y_GE65 age-band extraction; re-running restored RO. Backpropagated into `compute_feminicide_rates.py`'s foreign-population denominators (B34) — re-ran, rates shift ~1-10% per year/origin (e.g. 2006 españa victim 0.23→0.24/100k, otro_pais 1.27→1.13/100k) since foreign stock is now ~1.7M higher; `data/processed/feminicide_rates_2006-2024.csv` and `docs/data/feminicides.json` regenerated | V42,B34 |
| B37 | 2026-07-23 | T72's first `mi-stock-region` drill-down implementation left the 5 dimmed "other region" line datasets without a `stack` id while `scales.y.stacked=true` (needed for the drilled region's stacked country bars) — Chart.js's autoscale summed every dataset sharing the y-axis into one combined total for the max-range calculation regardless of stack grouping, inflating the axis to ~2-3x any real value (e.g. drilling into Latin America, whose own bars top out at 2.4M, produced a 4.5M axis — matching the sum of the 5 *other* regions' own maxes, ~4.16M). Fixed by giving each line dataset its own unique `stack` id so Chart.js treats it as an independent singleton group. Separately (same root class of bug): even after that fix, the axis was still sized to the union of the drilled region AND the dimmed lines, so a small region (Non-EU Europe, ~350k) rendered as a sliver under an axis sized for Latin America/EU's multi-million-strong lines — fixed by hard-capping `y.max` to the drilled region's own bar total (`barAxisMax()`), letting oversized dimmed lines clip at the top instead. Caught via direct Playwright inspection of `chart.scales.y.max` and the actual dataset values, not by eyeballing the rendered chart alone — the first version's screenshot looked plausible at a glance | V44,T72 |
| B39 | 2026-07-29 | Two regressions in `src/crime/analyze_cohort_crime_rate.py` (and 4 sibling scripts — `parse_ine_tabla28716.py`, `analyze_rape_trend_nationality.py`, `plot_sexual_crime_trends.py`, `analyze_aggression_profile.py`) left the whole "done" T41 output unreproducible without anyone noticing: (1) `ROOT = Path(__file__).parent.parent` was correct only when these scripts lived one directory below repo root; the "Modularize src" reorg (`ad7e3b2`, moving them to `src/crime/`, `src/sexual_crimes/`, `src/analysis/`, two directories deep) left `ROOT` resolving to `src/` instead of the repo root, crashing every one of these scripts with `FileNotFoundError` before they read any data. Fixed to `Path(__file__).parent.parent.parent`. (2) Independently, `load_migration_totals()`'s stock branch filtered `series == "stock_foreign_nationality"`, a series name that no longer exists for Morocco/Algeria after T66's expansion renamed it to `stock_nationality` — the function silently returned an empty dict for the "stock" key (`migration[(code, "stock")]` then `KeyError`'d in `main()`), and even before that rename the same branch never filtered `age_group` to `AGE_BANDS_15_59` despite the function's own docstring/dict-key naming ("male_15_59_total"), so it would have summed the *entire* population (ages 0-99+) rather than the working-age band the rest of the module (and the Spanish-population comparison, `load_population_totals()`) correctly uses. Both fixed. Verified by rerunning: results reproduce the originally-published T41 figures almost exactly (e.g. Algeria's 2024 rate ratio 1.9010886... vs the previously-cited 1.90, unchanged to 3 significant figures; all 4 output CSVs show the same row counts with only ±1-count-level differences, not a systematic shift) — a clean regression repair, not a methodology change. Found while extending T41 for the Algeria/Morocco divergence investigation (T76-T79); the other 4 scripts share the same `ROOT` bug and were fixed identically but not independently re-verified against a "before" baseline the way `analyze_cohort_crime_rate.py` was. | V33,C1 |
| B38 | 2026-07-26 | `data/raw/sexual_crimes_mir_2019-2024.json`'s 2020 report's victim-nationality table (`nationality.victims.by_country`, from `MIR_Informe_DelitosSexuales2020.pdf` page 19) sums to 429 (310 Spanish + 119 foreign) — confirmed via direct PDF text dump to be exactly what that page prints, titled "Año 2020," yet ~31x smaller than the year's 13,174 headline crime count. Every other year's equivalent table closely matches its own headline (2019: 15,706 vs. 15,319 headline, a normal victims-vs-crimes denominator difference); 2020's own perpetrator-nationality table on the same report is fine (7,959, a plausible annual figure). Not a parser bug — pdfplumber extracts exactly what the page contains — but a source-document defect specific to that one page/year. Surfaced while building T75's victim/perpetrator-nationality-by-region drilldown, where it would otherwise have plotted as a fake ~97%-of-year dip across every region. `build_dashboard_data.py`'s `_nationality_breakdown()` now nulls 2020 for the `victims` side only (`BAD_NATIONALITY_YEARS`), rendering as a genuine gap in the line/bar rather than a fabricated near-zero point. | V23,T75 |

---

## §V — Invariants added this session

V37: When `SPEC.md` is split into per-module files, every `§T`/`§V`/`§I` entry ! be traceable to exactly one file via the module-map table in §T above; this map ! be kept in sync whenever a task/invariant/interface entry moves between files.

V38: ∀ Chart.js category-scale annotation (`vline()` xMin/xMax) → its type (string vs number) ! match the chart's own `labels` array element type exactly, ∵ a string only resolves via `scale.parse`'s strict-equality label lookup while a number is treated as a raw index, never a label value; type mismatch ⊥ silently misplace the marker (label lookup miss appends a phantom trailing category → marker renders past all real data).

V42: Feminicide-rate population denominators (Spanish-resident vs foreign-resident split) ! derive from the real per-year per-nationality sex-specific stock in `migration_spain.csv`'s `stock_nationality` series (Eurostat `migr_pop1ctz`), not from a constant share ratio applied to a single year's sex split. `stock_nationality` carries the joint age(5yr)×sex×citizenship cross for 50 nationalities covering ~86% of INE ECP foreign stock; the residual ~14% (small nationalities not in the top-50 list) is a known coverage gap documented in the source notes. ! apply a frozen ratio across years as a population estimate.

V43: Wald 95% CIs on raw counts (count ± 1.96·√count) ! be presented as a useful uncertainty measure for feminicide rates — the CI captures only Poisson count variance (trivially narrow for N=24-50), not population-denominator uncertainty, and adds visual noise without analytical value. CI computation, CSV columns (`ci_lower`/`ci_upper`), dashboard tooltip fields, and HTML caveat text are all removed (B35).

V44: any dashboard drill-down series (a finer breakdown of an already-displayed aggregate — migration's T72 per-country lines under `mi-stock-region`'s per-region lines, or sexual-crimes' T75 per-country nationality breakdown under its region lines) ! be derived by summing the same underlying rows the aggregate itself sums, not computed as a separately-maintained series — guarantees the drill-down's parts always sum exactly to the aggregate line, with no double-source-of-truth drift possible between the two views. Cross-cutting (moved here from `SPEC-migration.md` once a second module, sexual_crimes, reused the same `regionDrilldownChart()` JS helper and pattern).
