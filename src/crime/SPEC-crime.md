# SPEC-crime.md — crime module

Part of the SVS spec. Top-level goal/shared constraints/invariants/roadmap/bug
log live in [`SPEC.md`](../../SPEC.md); this file owns §T tasks T4, T26-T31,
T37-T41, T45-T48, T50, T51, T59, T76-T88 and this module's domain-specific
§V/§I content only. Nationality/peligrosity, hate-speech-migration
correlation, and hate crimes all live here per the module map — "crime" as
the user named it.
---

## §I — Interfaces (crime-specific)

```
file: data/raw/hate_crimes_mir_2016-2021_2023.json     → MIR "Informe de Delitos de Odio" series (2016-2021, 2023; 2022 has no dedicated PDF, see B28), 13-category ámbito breakdown + 3-tier total (TOTAL DELITOS / INFRAC. ADM. / TOTAL DELITOS E INCIDENTES DE ODIO, 2019+); generated by T59's `OdioParser`/`run_odio_batch` (`--mode odio`)
file: data/raw/migrant_crime_numerator.csv             → crime/conviction counts by nationality (Spanish/foreign), offence type, age, sex — extracted from MIR Informes + INE Condenados (generated by T26/T27; T27 group-sexual-violence slice done, 43 rows, general-crime Balance de Criminalidad slice still pending)
file: data/processed/migrant_crime_rates.csv           → absolute per-100k crime rates by nationality × demographic slice, with CIs (generated by T28, planned)
file: data/raw/perpetrators_sexual.csv                 → distinct-perpetrator counts (sexual), deduped numerator for the peligrosity bracket (T30, planned)
file: data/processed/peligrosity_rates.csv             → bracket [convicted, identified] per 100k men, by age × nationality × origin, with CIs (T31, planned)
file: data/processed/ine_condenados_28716_sexual_crimes.csv, ine_condenados_28716_nationality_pct.csv → INE table 28716 convictions by nationality (T26,T30)
file: data/processed/cohort_tenure_*.csv (4 files) + 2 PNGs → Morocco/Algeria post-2022-cohort tenure hypothesis tests A-E (T41)
file: data/processed/mir_migration_rates.csv           → MIR reports × migration flux correlation (T50)
cmd:  `python src/parsers/mir_migrant_nationality_parser.py`     → extract MIR group-sexual-violence + reference PDFs → `data/raw/migrant_crime_numerator.csv` (T27)
cmd:  `python src/parsers/mir_parser.py --mode odio --pdf-dir <dir> [--out-dir <dir>]` → parse "Informe de Delitos de Odio" PDFs (content-located table, chart-rendered — word-clustering extraction, not `extract_tables()`) → one `MIRReport`/year, 2022 correctly skipped (no PDF exists, see B28) (T59)
cmd:  `python src/compute_migrant_crime_rates.py`      → numerator ÷ denominator → per-100k rates with CIs (planned, T28)
cmd:  `uv run python src/crime/parse_ine_tabla28716.py`           → INE table 28716 (T26,T30)
cmd:  `uv run python src/crime/analyze_cohort_crime_rate.py`      → cohort tenure hypothesis tests A-E (T41)
cmd:  `uv run python src/crime/analyze_mir_migration_correlation.py` → MIR/migration correlation (T50)
cmd:  `uv run python src/crime/analyze_rape_trend_nationality.py` → rape trend & nationality report, stdout only (T51)
file: `src/crime/build_dashboard_data.py`              → `build_hate_crimes()`/`build_cohort_tenure()`/`build_victim_vulnerability()`/`build_regularization_sensitivity()`/`build_general_crime()` consumed by `src/analysis/build_dashboard.py` → `docs/data/hate_crimes.json`, `docs/data/cohort_tenure.json`, `docs/data/victim_vulnerability.json`, `docs/data/regularization_sensitivity.json`, `docs/data/general_crime.json` (T41,T59,T76,T82,T85,T88)
doc:  `data/sources/mir_delitos_odio.md`               → MIR "Informe de Delitos de Odio" source doc — per-year hand-verified ámbito figures 2016-2023, 2017 discapacidad→diversidad-funcional methodology break, 2022 publication gap, chart-rendered-table extraction method, mixed thousands-separator formatting caveat (T59); plus the SES-portal nationality source description + closed-leads notes (T76)
file: data/raw/hate_crimes_ses_nacionalidad_{detenidos,victimas}_2021-2024.csv, ..._summary_2021-2024.csv → SES portal tables 06019/06013, national-level, per-country + España/foreign/%Spanish summary by (año, ámbito) — generated by T76's `parse_ses_odio_nationality.py`
cmd:  `uv run python src/crime/parse_ses_odio_nationality.py` → fetch + parse SES portal hate-crime nationality megatablas 06019 (detenidos)/06013 (víctimas) → the two CSV pairs above (T76)
doc:  `data/sources/lgtbifobia_espana.md`              → OSIG-specific hate-crime source doc, ported from `origin/claude/lgbtbi-aggressions-research-xfv5cy` and corrected: the branch's "no national OSIG-specific nationality crosstab exists" finding was wrong — it exists in the SES portal microdata, added as a correction (T76)
file: `data/processed/ine_condenados_28857_age_nationality.csv` → INE table 28857 (Condenados por delitos sexuales según sexo, edad y nacionalidad) — age(9 bands)×sex×nationality-REGION (not country) × year, 2017-2025; general-population reference age curve for T78's standardization test (T77)
cmd:  `uv run python src/crime/parse_ine_tabla28857.py`          → fetch+parse INE table 28857 (T77)
file: `data/processed/age_standardized_rate_test.csv`, `age_standardized_dz_ma_ratio.csv` → indirect age-standardization (SIR) of Morocco/Algeria sexual-crime rates against a general-population reference age curve, + the DZ/MA SIR-ratio-vs-crude-ratio comparison that actually tests H3 (T78)
cmd:  `uv run python src/crime/compute_age_standardized_rate.py` → T78 (see `reports/algeria_morocco_divergence.md`)
file: `data/processed/offense_subtype_funnel_triangulation.csv` → exploratory, heavily-caveated cross-reference of INE convicted-offense-subtype mix (region-level nationality) against MIR's within-Africa country shares (T79)
cmd:  `uv run python src/crime/analyze_offense_subtype_funnel_triangulation.py` → T79
file: `data/processed/victim_vulnerability_rates.csv`  → sexual-crime victim counts per-100k by nationality (female + total-population denominators), mirroring the peligrosity concept for victims (T81)
cmd:  `uv run python src/crime/compute_victim_vulnerability_rates.py` → T81
file: `data/raw/regularization_2026.csv`               → 2026 regularización extraordinaria application share by nationality, sourced (El Español 2026-07-02, corroborated top-5 vs official Moncloa release); feeds N20/T85
file: `data/processed/regularization_sensitivity_test.csv`, `regularization_sensitivity.png` → upper-bound denominator sensitivity scenario (assumes full regularization pool already present, 100% aged 15-59) for Morocco/Algeria/Colombia/Venezuela (T85)
cmd:  `uv run python src/crime/compute_regularization_sensitivity.py` → T85
doc:  `reports/algeria_morocco_divergence.md`          → synthesis of T41 + H1/H2/H3 hypothesis tests (T77-T80, T85, N20)
file: `docs/data/migration.json`'s `stock_projection_2026` key → illustrative 2026 stock projection, dashboard panel `mi-stock-2026-projected` (T86, built by `src/migration/build_dashboard_data.py`)```

## §V — Invariants (crime-specific)

V14: ∀ subgroup-relative-rate claim (e.g. "X% of crimes/perpetrators are nationality N") → ! paired with (a) the absolute base rate = count(N) ÷ population(N) per 100k, and (b) the population denominator + its source/year. Relative framings ⊥ published standalone — they obscure that an elevated relative rate can still correspond to a tiny absolute share (e.g. 3% absolute incidence vs 1% is a 3x relative difference but both ⊥ "large"). Directly targets B8-class claims (Haiku "32% extranjeros → 3.43x more likely", row 74 "37.3% foreign perpetrators").
V15: peligrosity ! reported as bracket [lower = convicted (INE Condenados), upper = identified (detenidos+investigados)] per 100k men of the slice; numerator = distinct persons (deduped), ⊥ crime-event counts (avoids one-perp-many-crimes inflation); denominator = male population of that slice (Padrón/ECP); subject to V14 relative/absolute pairing.
V16: any conversion between victimization rate and peligrosity ! account for one-aggressor→multiple-victims and repeat victimization via the §E relationship distribution; adjustment factor + source ! documented.
V18: Hate-speech datasets ! prioritized from established watchdog organizations (e.g., RAXEN, SOS Racismo, CEAR) + primary Interior/Fiscalía sources; ∀ correlation with migration data/crime ! explicitly document definitions, causality limits (associative only), and data limitations (survey coverage/reporting bias).
V45: ∀ residual/decomposition test that infers one sub-population's rate by holding a *different* sub-population's rate FIXED at a historical baseline (e.g. T41 Test B's settled/cohort split) → ! first check whether an independently-measured trend for a comparable reference population already exists in this repo (e.g. Test E's Spanish-population rate) and apply it as an adjustment, or explicitly justify in the code/docstring why a frozen baseline is preferred instead. A frozen sub-rate silently attributes 100% of any shared, non-group-specific trend to whichever bucket absorbs the residual, inflating its apparent effect (B43). Also: any hidden/undocumented population added as a sensitivity bound (no ground-truth tenure data) ! be assigned to whichever sub-bucket the assignment reasoning actually supports (stated explicitly), not defaulted to whichever bucket is computationally convenient (B42 defaulted to settled without justifying it; B43 corrected to cohort with reasoning stated).
- Primary source trace: CEAR report "Los delitos e incidentes de odio, especialmente aquellos relacionados con racismo y xenofobia, aumentaron un 23,6 % en 2025, la mayor cifra desde que hay registros del Ministerio de Interior" ! validated against Ministry of Interior annual hate-crime statistics.
- Victimization/Perpetrator balance: Analysis ! encompass comparative crime rates by different migrant populations, and crimes suffered *by* those populations (including systemic discrimination, institutional harm, and police practices) — using watchdog reports (e.g., reports on police profiling/institutional racism) alongside official statistics.

## §T — Tasks (crime)

| id | status | track | task | cites |
|---|---|---|---|---|
| T4 | . | A,B | Populate `violence_spain.csv` — non-sexual domestic violence denuncias: Ministerio del Interior / CGPJ 2000–2024 | V1,V2 |
| T26 | x | D | Extract perpetrator-nationality data (sexual violence) — DONE: `parse_country_breakdown_table`/`CountryBreakdown` in `mir_parser.py` (TDD, `tests/test_mir_parser_nationality.py`, 11 tests green) extracts every region/country row for both victims & perpetrators across all 5 years (2019,2021,2022,2023,2024), incl. 2024's reversed sex-column order and no-sex-breakdown perpetrators table; wired into `InformeParser._extract_nationality` + `NationalityBreakdown.by_country`; regenerated `sexual_crimes_mir_2019-2024.json`, spot-checked (2023 perp Marruecos: male 975/female 26/total 1001/pct 7.3 ✓). REMAINING: replace flagged row 74 (37.3% foreign, confidence=medium) using this data; cross-check INE Condenados table 28716 for category-specific figures (out of scope this phase) | C1,C13,V1,V2,V12,V14,V23 |
| T27 | ~ | D | Extract perpetrator-nationality data (general crime) — MIR Balance de Criminalidad / Anuario "detenidos/investigados por nacionalidad" totals (broader than sexual violence only) → headline absolute-rate numerator, appended to `data/raw/migrant_crime_numerator.csv`. PARTIAL: sexual-violence-adjacent group data done via `src/parsers/mir_migrant_nationality_parser.py` (43 rows, `migrant_crime_numerator.csv`); broader general-crime Balance de Criminalidad/Anuario numbers per original scope still pending | C1,V1,V2,V12,V14 |
| T28 | . | D | Compute absolute migrant crime rates — `src/compute_migrant_crime_rates.py`: join numerator (T26/T27, crime counts by nationality) ÷ denominator (`migration_spain.csv` stock by nationality/origin/age/sex, supplement with INE table 36825 if finer joins needed) → per-100k rates with binomial/Poisson CIs, by nationality and by demographic slice (origin region × age × sex) → `data/processed/migrant_crime_rates.csv` | V6,V14,C10 |
| T29 | . | D | Dashboard/report — present absolute migrant-crime-rate analysis pairing relative and absolute framings side-by-side (e.g. "37% of detentions ↔ N per 100k residents vs M per 100k for nationals — both small in absolute terms"); add dashboard section + `reports/migration_crime.md` write-up addressing the "migrants commit more crime" generalization directly with sourced absolute base rates by demographic slice | I.*,V14,C8 |
| T30 | . | D | Extract distinct-perpetrator counts (sexual) — MIR Informe detenidos+ investigados 2019–2024 (deduped where possible) + INE Condenados table 28716 by offence/sex/age/nationality → `data/raw/perpetrators_sexual.csv` (numerator for peligrosity bracket; generalizes T26). | C1,C13,V1,V12,V14,V15 |
| T31 | . | D | Compute peligrosity → `src/compute_peligrosity.py`: distinct perps ÷ male population (Padrón/ECP) → bracket [convicted, identified] per 100k men, by age × nationality × origin, with CIs → `data/processed/peligrosity_rates.csv`; nationality slice ! reconcile with `migrant_crime_rates.csv` (T28). | V6,V14,V15,C10 |
| T37 | . | F | Collect hate-speech-watchdog datasets (RAXEN, SOS Racismo) & primary source data (Interior/Fiscalía/INE) on hate-crimes/incidents (racism/xenophobia 2025 spike). | V18 |
| T38 | . | C,F | Map correlation series: migration rates (T11), public polling (CIS worry), xenophobic party voting shares (CIS/Interior). | V18,C8 |
| T39 | . | F | Analyze media presence of hate-speech/xenophobia: literature review/content analysis (if available via watchdog reports). | V18 |
| T40 | . | F | Write `reports/hate_speech_analysis.md` — synthesize hate-speech findings, correlate with migration/crime, address associations. | V18 |
| T41 | x | D,C | Test Morocco/N.Africa post-2022-cohort tenure hypothesis, extended to South America/EU-Europe (share only) — DONE: `src/crime/analyze_cohort_crime_rate.py`, five formal hypothesis tests (A: period rate-ratio, B: cohort-specific decomposition, C: share-of-total, D: Test A per-100k, E: diff-in-diff vs Spanish population). Headline: Marruecos/Argelia total-group rates significantly elevated post-2022 (Test A); vs the Spanish population's own rising baseline, Marruecos's rise is significantly smaller and Argelia's significantly larger in most comparisons (Test E). South America's crime share rose significantly from 2023 on; EU-Europe's fell by 2024 (Test C only — no population time series exists yet for these groups, see T43-T48). Also added: a deeper `pre_2020_pooled_2017_2019` baseline (real data since T83/T87) run across Tests A/B/C/E alongside the original. **Test B REVISED TWICE (B42 then B43, both 2026-07-30, see SPEC.md)**: the original decomposition (gross-inflow cohort proxy + a tautological settled-baseline rate) made "not concentrated in the newly-arrived cohort" an artifact (B42 fix: net-stock-delta split + baseline rate anchored to the group's own total pre-surge rate — flipped the headline to "cohort significantly ABOVE settled", but produced implausibly large 4x-6x ratios). B43 fixed two further issues the first fix introduced/left standing: the regularization-sensitivity hidden population is now assigned to `cohort_pop` (recent arrivals — the more defensible read of why an extraordinary, lower-bar amnesty was needed at all), not `settled_pop`; and the settled sub-population's expected rate is now scaled by the Spanish reference population's own documented rate trend (Test E) instead of frozen at the historical baseline, which had been silently loading the whole society-wide rate rise onto the tiny cohort bucket. **Current result**: Marruecos shows NO significant cohort elevation in any comparison (consistent with Test E's finding that its overall rate rose less than Spain's); Argelia shows a real, moderate, significant cohort elevation specifically by 2024 (ratio 2.3x-3.6x depending on baseline/regularization-adjustment; non-significant 2022-2023) — consistent with Test E's finding that its overall rate rose more than Spain's. New invariant V45 (this file) + regression tests `tests/test_analyze_cohort_crime_rate.py`. **Test F added (2026-07-30, user request)**: Test B's rolling 3-year window redefines "recent" relative to every observation year; Test F instead uses a single, fixed calendar cutoff (settled_pop pinned once at `stock(cutoff_year - 1)`, not recomputed per year), run for cutoff∈{2022, 2024}, baseline = every real MIR year strictly before that cutoff (deepest available), same Spanish-trend adjustment as Test B. RESULT: Argelia shows a significant elevation at cutoff=2022/test=2024 (3.61x, p=.0002) and cutoff=2024/test=2024 (4.59x, p=.0003) — converges with Test B's own 2024 finding via an independently-designed method, reinforcing it's not a rolling-window artifact; Marruecos shows no significant elevation anywhere, also converging with Test B. Caveat: cutoff=2022/test=2022 has a near-zero cohort_pop (e.g. Argelia: 66 people, one year's accumulation since the cutoff), making that one cell's point estimate (52x, still non-significant, p=.36) extremely unstable — flagged in the CSV/chart (log-scale, annotated with cohort_pop, not hidden) rather than silently trusted. The regularization-adjusted variant (pool assumed to have arrived at/after the cutoff, added to cohort_pop) implies an extreme undocumented share of that period's net growth (79%-99.75% depending on cell) — a far more extreme upper bound than Test B's own regularization variant, precisely because Test F's accumulation windows are shorter; nearly every regularization-adjusted cell reads "significantly BELOW baseline" as a mechanical consequence, not a considered best estimate (V14). Full methodology, all RESULT figures, and every caveat/UPDATE: `reports/cohort_tenure_analysis.md`. | V6,V9,V14,V15,V25,V45,C8,C9,C10 |
| T45 | . | D | Run Test A (T41 methodology: period-level two-sample Poisson rate-ratio z-test, whole-group crime-count ÷ male-15-59-stock rate, pre-2022 vs post-surge years) for the South America and EU-Europe groups once T43/T44 land real population data — re-run `src/crime/analyze_cohort_crime_rate.py`'s Test A runner (already generic over group/code/stock/crime dicts, no code change needed) per new group; extend `cohort_tenure_period_test.csv` + `cohort_tenure_rate_ratio.png`. Depends on migration's T43, T44. | V6,V9,V14,C8 |
| T46 | . | D | Run Test B (T41 methodology: cohort-specific decomposition — crime(year) = r_settled_baseline·settled_pop + r_cohort·cohort_pop, isolates whether newly-arrived sub-population specifically is elevated vs settled) for the South America and EU-Europe groups once T43/T44 land real population data — re-run Test B runner per new group; extend `cohort_tenure_rates.csv` + chart. Depends on migration's T43, T44. | V6,V9,V14,V25,C8 |
| T47 | . | D | Run Test D (T41 methodology: Test A restated per 100,000 males 15-59, presentational unit conversion only, same statistic/p-values) for the South America and EU-Europe groups — trivial once T45 lands (adds `rate_baseline_per_100k`/`rate_test_per_100k` columns to the Test A CSV for the new groups). Depends on T45. | V6,V14,C10 |
| T48 | . | D | Run Test E (T41 methodology: diff-in-differences / log-linear Poisson interaction z-test — is the group's rate ratio significantly different from the Spanish male 15-59 population's rate ratio over the same periods) for the South America and EU-Europe groups once T43/T44 land real population data — re-run Test E runner per new group; extend `cohort_vs_spanish_test.csv` + chart. Depends on migration's T43, T44. | V6,V9,V14,V15,C8 |
| T50 | x | C,D | MIR reports × migration flux correlation analysis — DONE: `src/crime/analyze_mir_migration_correlation.py`. Examines whether sexual-crime growth/foreign-perpetrator share outpaced migration growth 2015-2024, and whether Spanish/foreign per-100k perpetrator rates converged or diverged; output `data/processed/mir_migration_rates.csv`. Discovered as an unwired orphan during the T53 parser-consolidation cleanup — code and output predate this task id. | V14,V25,C8 |
| T51 | x | B,D | Rape trend & nationality analysis — DONE: `src/crime/analyze_rape_trend_nationality.py`. Answers: year-by-year reported-rape trend to 2024; convicted-sex-offender nationality distribution 2017-2024 (via `parse_ine_tabla28716.py`'s table-28716 extract); whether the "immigration drives the increase" claim holds against known-vs-unknown-perpetrator reporting-rate context. Stdout report only, no file output. Discovered as an unwired orphan during the T53 parser-consolidation cleanup. | V14,C1 |
| T76 | x | C,D | Hate-crime nationality by ámbito (2021-2024) + close two false leads — DONE: two leads chased and closed first (`MIR_BalanceCriminalidad_2023_Q4.pdf`, 501 pages, full-text scanned, zero nationality/odio matches; INE table 26014 + full 112-operation catalogue, no hate-crime/Título-XXI category anywhere) before finding the real source — the SES portal (`estadisticasdecriminalidad.ses.mir.es`) publishes 20 queryable PC-Axis megatablas (`06001`-`06020`) behind the PDF charts, no Cloudflare block. `src/crime/parse_ses_odio_nationality.py` fetches tables `06019` (detenidos/investigados) and `06013` (victimizaciones), filters to national level, handles the two tables' different column layouts (7 vs 6 cols) and different "total sex" labels (`Ambos sexos` vs `TOTAL sexo`). Validated exact-match against known figures (2023 detainees 78.7% Spanish, victims 62.2%, both matching prose figures in `discurso_odio_inmigracion_espana.md`) and found a new one (OSIG-specific detainees 72.1% Spanish, 2023 — previously believed not to exist at national level, see below). Coverage confirmed 2021-2024-only via a second, non-nationality table (`06001`), so this is a portal-wide limit, not nationality-specific. Extended `classify_odio_category` for 2 new ámbito labels (`DISFOBIA`→`discapacidad` rename, `ISLAMOFOBIA` new key). Wired into `build_hate_crimes()` → `docs/data/hate_crimes.json`'s new `nationality` block, and a new `hc-nationality` chart in `docs/app.js`/`docs/index.html`. Ported `data/sources/lgtbifobia_espana.md` from `origin/claude/lgbtbi-aggressions-research-xfv5cy` (predates `src/` modularization, couldn't merge directly) with a correction: that branch's "no national OSIG-specific nationality crosstab exists" conclusion was wrong, now fixed with the real series. 8/8 new tests (`tests/test_ses_odio_nationality.py`), full repo suite green, no regression. No official conviction (condenados) series exists for hate crimes — confirmed from 3 independent angles (INE, SES portal, prior OSIG research); documented as a caveat, not worked around. | V12,V14,V19,C1,C13 |
| T77 | x | D | Morocco/Algeria age-composition data (H3) — DONE: INE table catalog searched for an age×nationality cross finer than table 28716 (crime-type × nationality-region only, no age); found tables 28857/28709, but both stop at continent-level nationality, so no country-level age×crime source exists. Built instead: `src/crime/parse_ine_tabla28857.py` (general-population reference age curve, T78's input) + `src/migration/build_dashboard_data.py`'s `_country_age_pyramid()`/`_working_age_composition_trend()` (Morocco/Algeria-specific, not region-level, age pyramid + % aged 15-39 vs 40-59 trend 2002-2025, real Eurostat data). Finding: Algeria's population aged sharply then rejuvenated post-2018 (bottomed ~41-42% aged 15-39 in 2017-2018, back to 57-60% by 2024-2025), converging with Morocco's shape almost exactly by 2023-2024 — the same period the rate gap is largest. Dashboard panels `mi-age-pyramid-dz-ma`, `mi-dz-ma-age-trend`. Full writeup: `reports/algeria_morocco_divergence.md`. | V6,V14,C10 |
| T78 | x | D | Indirect age-standardization (SIR) test of H3 — DONE: `src/crime/compute_age_standardized_rate.py`. Applies a general-population reference age-specific conviction-rate curve (INE 28857 `nationality=total` ÷ exact single-year population) to Morocco's/Algeria's own age distributions (Eurostat) to get an age-standardized expected count, compared to observed MIR counts. Caveated: reference curve is convicted (INE), comparison numerator is investigated (MIR) — different funnel stages, so absolute SIR values (10x-27x) are dominated by that gap; the DZ/MA *ratio* of SIRs (which cancels the shared funnel bias) is the informative statistic. RESULT: age-standardization widens, not narrows, Algeria's gap over Morocco in every year tested (2019-2024) — H3 does not explain the divergence, consistent with T77's population-shape convergence finding. Output `age_standardized_rate_test.csv`, `age_standardized_dz_ma_ratio.csv`. | V6,V14,V15,C10 |
| T79 | x | D | Exploratory funnel triangulation (convicted-subtype vs investigated-nationality) — DONE: `src/crime/analyze_offense_subtype_funnel_triangulation.py`. Heavily caveated (funnel-stage mismatch + region-vs-country granularity mismatch, association only per V9): compares INE 28716's Africa-region convicted-subtype mix against MIR's within-Africa Morocco/Algeria perpetrator shares. Found the Africa subtype mix shifted sharply post-2022, but this tracks the LO 10/2022 abuso→agresión legal reclassification (C3) far more plausibly than any nationality-linked signal — no reliable subtype-by-country conclusion drawn. | V9,V14,C1 |
| T80 | x | D | Synthesis report — DONE: `reports/algeria_morocco_divergence.md`. Restates T41's headline, reports T77/T78's H3 result (age-adjustment widens not narrows the gap), H1's legal-pathway research (real: Spain-Morocco has bilateral labor/residence agreements since 1996/2001 + ongoing GECCO seasonal-worker program; Spain-Algeria has none, only tax/investment treaties), N20's H2 result (Algeria's 2026 regularization-application share is ~2.5x its registered-population share, vs Morocco's ~0.9x — a real supporting signal for H1), T85's sensitivity scenario (even the maximal over-corrected denominator only narrows Algeria's 2024 rate excess over Morocco from 2.43x to 1.75x, not to parity), and T79's exploratory triangulation. Verdict: H1/H2 together partially explain the divergence; a real residual gap remains unexplained. Updated after user pushback surfaced a fuller regularization ranking + requested concrete-numbers framing for H3 (see B40 for a related WebSearch-synthesis caution). | C1,V14,V9 |
| T81 | x | D | Victim vulnerability rate (mirror of peligrosity, victim side) — `src/crime/compute_victim_vulnerability_rates.py`: MIR victim-by-country counts (2020 excluded, B38) ÷ Eurostat female/total nationality stock → per-100k rate with CIs, `data/processed/victim_vulnerability_rates.csv`. | V6,V14,C10,C12 |
| T82 | x | D | Victim vulnerability dashboard wiring — `build_victim_vulnerability()` in `src/crime/build_dashboard_data.py` → `docs/data/victim_vulnerability.json`; panel `sx-victim-vulnerability-rate` in the Sexual Crime tab (rate-not-share companion to `sx-nationality-victims`). | I.*,V14 |
| T83 | x | D | Extend nationality-specific crime data further back — DONE, but via a different route than originally planned. Verified directly against the Anuario PDFs (2016-2023): confirmed Anuario has NO nationality-breakdown table at all for crime data — its only foreigner-related table (`TABLA 3-1-8/3-1-9 "DETENCIONES E INVESTIGADOS EXTRANJEROS"`) splits Spanish-vs-foreign by sex and by comunidad autónoma/province, never by country of origin (checked every page of the 2019 edition's "3.1.4 EXTRANJEROS" section by hand). Anuario is a dead end for this, as T83 itself anticipated. Instead found the real fix: `MIR_Informe_DelitosSexuales2017.pdf`/`2018.pdf` were already staged in `data/sources/` but excluded from the dataset (`data/sources/mir_informes_delitos_sexuales.md`: "2017/2018 Informe PDFs failed to parse cleanly on their older layout — 2017 returned empty, 2018 returned an internally-inconsistent total"). Direct inspection showed the FAILURE was isolated to the headline "hechos conocidos" typology table (older 2012-2017/2013-2018 multi-year layout, different from 2019+'s per-category table); the SAME editions' "3 Perfil de la VÍCTIMA: sexo y nacionalidad" / "4 Perfil del RESPONSABLE: sexo y nacionalidad" tables (`_locate_nationality_table`/`parse_country_breakdown_table`, already built for T26) parse cleanly with full country-level breakdown for both years — confirmed by direct extraction before touching any code (2017 perpetrators: Marruecos 309, Nigeria 45, Argelia not yet individually named that year, folded into "Resto"; 2018 perpetrators: Marruecos 564, Argelia 72 individually named, Nigeria 29). Fixed in `mir_parser.py` (see T87 for the exact code change) so this data isn't silently dropped just because the unrelated typology table fails; regenerated `data/raw/sexual_crimes_mir_2017-2024.json` (superseding `..._2019-2024.json`, deleted) with real country-level nationality data now starting 2017 instead of 2019 for both victims and perpetrators — 2 more years of exactly the "crime data by nationality" this task asked for. `total_count` for 2017/2018 stays `None` (headline typology genuinely unparseable, not guessed at); the Anuario series remains the authoritative total-count source for those years. | C1,V12,C9 |
| T84 | x | B,D | General (non-nationality) long-run per-capita + Spanish-vs-foreign detention-rate trend for homicide/robbery/sexual-assault — DONE, scope narrowed to Spanish-vs-foreign (not "2000s→present") per explicit user direction after T83 confirmed no per-country breakdown exists in any general-crime MIR source either. `src/crime/parse_anuario_general_crime.py`: extracts 2 tables per Anuario edition (2016-2023) from the "3 SEGURIDAD CIUDADANA" chapter — (a) "INFRACCIONES PENALES. HECHOS CONOCIDOS/DETENCIONES E INVESTIGADOS. TOTAL NACIONAL" (ALL-nationality, a *5-year rolling window* per edition, so 8 editions combine into full 2012-2023 coverage with no gaps) and (b) "DETENCIONES E INVESTIGADOS EXTRANJEROS POR CAUSA DE INFRACCIÓN PENAL" (foreign-only, sex-split, 2-year window per edition, combining into full 2015-2023 coverage). 3 categories per table: homicide ("1. Homicidios dolosos/asesinatos", incl. attempts, not just "consumados"), robbery ("2. Robos con fuerza en cosas" + "3. Robos con violencia o intimidación" summed, excludes "hurto"/simple theft), sexual_assault ("III. Contra la libertad sexual" / "III. Libertad sexual", same headline already cross-validated elsewhere against the Informe series). Real engineering cost: 2020+ editions restructured this whole chapter — different table titles ("SERIE HISTÓRICA" → "EVOLUCIÓN"), different column headers ("Sexo Masculino/Femenino" → bare "Hombre/Mujer"), tables spanning multiple PDF pages with no repeated header (silently dropping the robbery rows when only the first page was read), and a title-keyword check that falsely matched a DIFFERENT table sharing this chapter's running page-header breadcrumb — none of these were solvable by hardcoding one page layout; fixed by locating tables via precise contiguous-phrase title regexes (not loose keyword-anywhere-on-page checks) and validating each extranjeros-table candidate page by attempting the real extraction rather than a pre-check heuristic (the heuristic false-matched a sibling subsection's cross-referencing intro prose on 2 different editions before this fix). `src/crime/compute_general_crime_trends.py`: per-capita rate = hechos_conocidos ÷ total Spain population (`population_spain_midyear_5yr.csv`); Spanish/foreign rate = detenciones_total/detenciones_foreign ÷ their respective populations (foreign = `migration_spain.csv` stock_foreign_nationality total; Spanish = total − foreign, derived not source-reported) — mirrors this dashboard's existing peligrosity convention (identified-perpetrator rate) rather than a reported-crime rate for the nationality comparison. Output `data/raw/mir_anuario_general_crime_2015-2023.csv` (153 rows) + `data/processed/general_crime_trends.csv` (90 rows). Confidence `medium` throughout (C16). | V6,V12,C9,C10,C16 |
| T85 | x | D | Regularization-sensitivity scenario — DONE: `src/crime/compute_regularization_sensitivity.py`. Upper-bound test: if the entire 2026 regularization-application pool for a nationality (a) had already been present throughout 2019-2024, (b) were 100% aged 15-59, (c) split male/female per that nationality's own real registered sex ratio (a genuine per-country estimate, not a flat 50/50 — split varies 31.6%-81.1% male across the 10 nationalities), how much would the peligrosity rate fall? Run for all 10 individually-named regularization nationalities (Morocco/Algeria/Colombia/Venezuela/Peru/Honduras/Paraguay/Senegal/Pakistan/Argentina). RESULT: correction is proportionally far larger for Algeria (denom +69.9% in 2024, rate −41.2%) than any other nationality; Morocco shows the smallest correction of all 10 (+22.5%, −18.4%) — Algeria and Morocco sit at opposite ends of the full distribution, not just relative to each other. Even so, Algeria's 2024 rate stays ~1.75x Morocco's post-correction (down from 2.43x) — narrows substantially, doesn't eliminate the gap. Senegal/Paraguay/Argentina/Pakistan have thinner MIR-year coverage (1-3 of 5 years, didn't always make that year's top-N cutoff). Output `regularization_sensitivity_test.csv` + `regularization_sensitivity.png`. | V6,V14,C10 |
| T86 | x | D | Illustrative 2026 registered-stock projection — DONE: `src/migration/build_dashboard_data.py`'s `_stock_projection_2026()`. Dashed/shadowed extension of each regularization-named nationality's real registered-stock trend line: the 2026 point = latest real stock (2025, or 2024 where 2025 isn't available) + that nationality's full regularization-application count, clearly flagged `is_projected` per point and captioned as illustrative-only (assumes 100% approval, no overlap with existing stock — neither holds in reality). Dashboard panel `mi-stock-2026-projected`, `confidence=low`. Not a forecast; addresses "what would registered stock look like if the regularization pool were simply added." | V6,V14,C10 |
| T87 | x | D,infra | `mir_parser.py` fix enabling T83 — DONE. (1) `InformeParser.parse()` now seeds a `total_sexual_crimes` placeholder record (count=None) before extraction runs, so `_extract_sex_breakdown`/`_extract_nationality`'s `_update_field` calls always have a record to attach to instead of being silently dropped when `_extract_typology` finds nothing (as for 2017) or (found during this fix) produces an internally-inconsistent total (2018: parsed 81 vs a 703 subcategory sum, off ~9x — a real latent bug, not hypothetical). (2) `_extract_typology` now skips entirely for `year < 2019` rather than risk shipping a wrong headline count, with an explanatory `notes` field pointing at the Anuario series instead. (3) `_upsert()` had a latent bug this surfaced: it never updated `source_page` on an already-existing record, only `count`/`notes` — harmless before (typology's `_upsert` call always created the record fresh) but would have silently frozen every record's `source_page` at `None` now that the placeholder-first seed always creates it first; fixed to update `source_page` too. (4) `main()`'s `--pdf-dir` glob for `--mode informe` was a bare `*.pdf`, unsafe against `data/sources/`'s mixed Informe/Anuario/Odio/Balance PDFs (would feed wrong-format files to `InformeParser` and risk `run_batch`'s V22 year-collision guard); scoped to `MIR_Informe_DelitosSexuales*.pdf`, matching odio mode's existing pattern. Verified via full regen: 2019-2024 records byte-identical before/after (0 diffs) once the `source_page` fix landed; `uv run pytest` 81/81 green. One legitimate downstream ripple, not a bug: `analyze_cohort_crime_rate.py`'s `load_perpetrator_counts()` backfills sex-less report-years (e.g. 2024, which has no per-country sex split for ANY nationality that year) using each country's own historical male-share average — Morocco's average now includes 2 more real data points (2017: 289/309 male, 2018: 548/564 male), shifting its 2024 backfilled male count from 1081→1072 (0.8%) and every DZ/MA cross-ratio that depends on it (`age_standardized_dz_ma_ratio.csv` 2024: SIR 2.53→2.55, crude 2.41→2.43 — `reports/algeria_morocco_divergence.md` updated). Algeria's own within-country rate ratio (the "1.90x by 2024" headline) is unaffected since Algeria already had a real (non-backfilled) sex split close to 100% male in both new years. Downstream wiring: 7 consumer scripts' `MIR_JSON`/`INFORME_JSON` constants repointed at the new filename; 2 of them (`sexual_crimes/build_dashboard_data.py`'s `_merge_totals`, `sexual_crimes/plot_sexual_crime_trends.py`'s `total_crime_by_year`) needed an explicit `total_count is not None` guard added so 2017/2018's `None` total doesn't clobber the Anuario-sourced total or crash the migration-correlation Pearson/scatter code; the other 5 (`analyze_cohort_crime_rate.py`, `compute_age_standardized_rate.py`, `compute_regularization_sensitivity.py`, `analyze_offense_subtype_funnel_triangulation.py`, `compute_victim_vulnerability_rates.py`) needed no logic change — either already keyed off per-country totals (never report-level `total_count`) or already restricted to an explicit hardcoded year list that doesn't include 2017/2018, so T41/T78/T79/T85's previously-published conclusions are unchanged except the one documented ripple above. `victim_vulnerability_rates.csv` (T81) and `sexual_crime_evolution.csv`'s nationality_country section (T42) now genuinely extend back to 2017 with zero code changes, since both already iterated every report year unconditionally. | C1,C9,V12,V33 |
| T93 | x | D | Fix B44: `compute_general_crime_trends.py build_rows`'s `sp_pop_adj` sourced from `population_spain_nationality.csv` (T89) directly instead of `sp_pop − for_pop` (T84's derived-not-source-reported Spanish denominator) | V46 |
| T94 | x | D | Fix B44: `analyze_cohort_crime_rate.py estimate_spanish_male_15_59` (feeds Test E `spanish_rate_for_years`, chained into Test B/F per B42/B43) sourced from `population_spain_nationality.csv` (T89) male-15-59 band directly instead of `male_15_59[y] − foreign_stock[y] * working_age_share`; orphaned `load_population_totals`/`load_foreign_stock_total` helpers removed (V37). Re-ran: Test A/C/D unaffected (0 hypothesis_call changes); Test B 1 cell + Test F 2 cells crossed the p=.05 boundary (all Marruecos, all moving further *away* from elevation, e.g. Test B deep-baseline 2024 ns→significantly-below; Test F cutoff=2024/test=2024 ns→undefined-negative-residual); zero Argelia cells changed call. Both published headlines ("Marruecos: no elevation", "Argelia: significant 2024 elevation") unchanged, just re-verified with corrected numbers. `reports/cohort_tenure_analysis.md` + `reports/algeria_morocco_divergence.md` updated with the corrected figures | V46,V25,V45 |
| T95 | x | D | Fix B44: `analyze_mir_migration_correlation.py`'s hardcoded `TOTAL_POP`/`FOREIGN_STOCK`-derived `spanish_pop_2023` corrected to the real t.56936 2023 "Española" figure (script stays hardcoded-constants per its documented PIPELINE.md convention — only the constant is fixed, not its live-CSV status) | V46 |
| T88 | x | D | General-crime dashboard wiring — DONE: `build_general_crime()` in `src/crime/build_dashboard_data.py` → `docs/data/general_crime.json`; per-capita series indexed to each category's own first year = 100 (raw rates differ by orders of magnitude across categories, V14: raw rate kept alongside in tooltip, not shown only as an index); foreign/Spanish series exposed as a rate ratio. Panels `gc-per-capita`/`gc-foreign-ratio` in the Migration tab's new "Long-run crime trends" section. | I.*,V14 |
| T96 | . | D | Investigate a country-level convictions lead for hate crimes — check whether the 14 Jul 2026 release about 4,531 adult convicted persons in the Central Registry of Sexual Offenders (sex split only) or any related CONDENADOS-by-country source can support a nationality/country split for hate-crime convictions, not just investigados/detenidos; if a primary table exists, add it, otherwise document the dead end and keep `mir_delitos_odio.md` / `lgtbifobia_espana.md` aligned. | V14,V18 |

## Related bugs

B7, B15, B19, B28, B39, B40, B41, B44 — see `SPEC.md` §B for full text.
